# Phase 2 架构优化：方案 A 实施计划

> **基准**: Phase 1 基础设施已交付，3683 passed  
> **策略**: 方案 A — 先低风险渐进推进  
> **原则**: 不改业务逻辑、不碰安全关键路径、每 Sprint 结束跑全量测试

---

## 范围总览

| Sprint | 任务 | 文件数 | 错误模式数 | 风险 | 建议顺序 |
|--------|------|--------|-----------|------|---------|
| Sprint 1 | API 错误处理迁移 | 14 个路由文件 | 66 处 try/except | 🟢 低 | 第 1 |
| Sprint 2 | routes.py 大拆分 | routes.py (88 端点) + 3 个已有 domain router | N/A | 🟡 中 | 第 2 |
| Sprint 3 | node_registry 插件化 | node_registry.py (1361 行) | N/A | 🔴 高 | 第 3 |

---

## Sprint 1: API 错误处理迁移

**目标**: 将 66 处 `except Exception as exc: raise HTTPException(400)` 替换为 `MedImageError` 子类

**背景**: 全局异常处理器已在 Phase 1 就位（`error_handler.py`），任何 `MedImageError` 子类会自动转为标准 JSON 响应。迁移只需替换 raise 语句。

### 当前状态：错误模式分布

```
routes.py                   23 处  ← 最多，含 88 个端点
dpabi_routes.py             16 处  ← 已创建但未激活的 domain router
project_routes.py            7 处
execute_reviewed_routes.py   5 处
dashboard_routes.py          4 处
gui_agent_routes.py          4 处
planner_routes.py            3 处
agent_routes.py              1 处  ← 已创建但未激活的 domain router
audit_record_routes.py       1 处
desktop_routes.py            1 处
external_smoke_routes.py     1 处
_shared.py                   1 处
─────────────────────────────────
approval_gate_routes.py      0 处  ← 无需修改
llm_planner_routes.py        0 处
plan_validator_routes.py     0 处
preset_routes.py             0 处
tool_catalog_routes.py       0 处
project_history_routes.py    0 处
rsfmri_routes.py             0 处
─────────────────────────────────
总计                         66 处
```

### 迁移模式

**替换前** (当前代码，在 14 个文件中重复 66 次):
```python
try:
    result = some_service.do_work()
except HTTPException:
    raise
except Exception as exc:
    raise HTTPException(status_code=400, detail=str(exc))
```

**替换后**:
```python
from src.backend.app.core.exceptions import PipelineError

try:
    result = some_service.do_work()
except MedImageError:
    raise
except Exception as exc:
    raise PipelineError(str(exc), details={"original_error": str(exc)}) from exc
```

### 分批执行计划

| 批次 | 文件 | 模式数 | 预计时间 |
|------|------|--------|---------|
| 1 | _shared.py → 先修共享模块 | 1 | 5 min |
| 2 | routes.py | 23 | 15 min |
| 3 | dpabi_routes.py | 16 | 10 min |
| 4 | project_routes.py | 7 | 8 min |
| 5 | execute_reviewed_routes.py | 5 | 5 min |
| 6 | dashboard_routes.py | 4 | 5 min |
| 7 | gui_agent_routes.py | 4 | 5 min |
| 8 | planner_routes.py | 3 | 3 min |
| 9 | agent_routes.py + audit_record + desktop + external_smoke | 各 1 | 5 min |

### 特别说明

- `_shared.py` 第 42 行是共享工具函数中的异常处理，修改后影响所有调用方，应最先处理
- 0 处模式的文件（approval_gate、llm_planner、plan_validator、preset、tool_catalog、project_history、rsfmri）无需修改
- 不需要修改 `api/models.py`（Pydantic 模型定义，无异常处理）

### 验收标准

- 全量测试通过（当前基线 3683 passed）
- 新增一个测试：API 端点抛 MedImageError → 返回标准 JSON `{ok, error{code, message, details}, request_id}`
- 确认所有 500 错误（未预期异常）被全局处理器捕获

---

## Sprint 2: routes.py 大拆分

**目标**: 将 routes.py 中 ~88 个端点迁移到各 domain router，删除 routes.py 中的重复端点

### 关键发现

routes.py 中的端点 **已经被复制到了独立的 domain router 文件中**，但 domain router 在 main.py 中 **被注释掉未激活**：

```
routes.py         ← 当前注册，包含全部 88 个端点
dpabi_routes.py   ← 已创建，20 个端点与 routes.py 完全重复
rsfmri_routes.py  ← 已创建，25 个端点与 routes.py 完全重复
agent_routes.py   ← 已创建，11 个端点与 routes.py 完全重复
```

main.py 第 13-18 行的注释说明：
```python
# Domain-specific routers (extracted from routes.py — activate once old
# endpoints are removed from routes.py to avoid route conflicts):
# from src.backend.app.api.dpabi_routes import router as dpabi_router
# from src.backend.app.api.rsfmri_routes import router as rsfmri_router
# from src.backend.app.api.agent_routes import router as agent_router
```

### 端点领域分析

routes.py 中 88 个端点的领域归属：

| 领域 | 端点数 | 目标 router | 状态 |
|------|--------|------------|------|
| DPABI | 20 | dpabi_routes.py | ✅ 已创建，端点已复制 |
| rs-fMRI | 25 | rsfmri_routes.py | ✅ 已创建，端点已复制 |
| Agent/Run/Retry/Scheduler | 11 | agent_routes.py | ✅ 已创建，端点已复制 |
| GPU | 4 | **gpu_routes.py** | ❌ 需新建 |
| Pipeline | 2 | **pipeline_routes.py** | ❌ 需新建 |
| Session/History | 6 | **session_routes.py** | ❌ 需新建 |
| Advisor | 5 | **advisor_routes.py** | ❌ 需新建 |
| Experiments | 6 | **experiment_routes.py** | ❌ 需新建 |
| Artifacts/Insights | 3 | **artifact_routes.py** | ❌ 需新建 |
| Real Data/Sandbox/Workflow | 6 | **realdata_routes.py** | ❌ 需新建 |
| Files/Logs/Bundle/Docs/Deploy/Release | 8 | 合并到 pipeline 或保留在 routes.py | ⚠️ 待定 |
| Health + Project-config | 2 | routes.py（保留） | ✅ 不迁移 |

### 执行步骤

**Step 0: 端点枚举快照**
```bash
# 生成 routes.py 完整端点清单
grep -n '@router\.' src/backend/app/api/routes.py > /tmp/endpoints_before.txt
```

**Step 1: 创建缺失的 domain routers**（7 个新文件）
- `gpu_routes.py` — GPU detect/benchmark/capability/synthetic-benchmark
- `pipeline_routes.py` — Pipelines list/detail
- `session_routes.py` — Sessions index/query/runs/nodes/search + History
- `advisor_routes.py` — Advisor protocol/error/qc-report/parameters/docs-qa
- `experiment_routes.py` — Experiments run-index/record/compare/dashboard
- `artifact_routes.py` — Artifacts list/preview + Insights
- `realdata_routes.py` — Real-data inspect/inventory/risk-report/protocol + Sandbox + Workflow

**Step 2: 激活已有的 domain routers**
```python
# main.py — 取消这 3 行注释：
from src.backend.app.api.dpabi_routes import router as dpabi_router
from src.backend.app.api.rsfmri_routes import router as rsfmri_router
from src.backend.app.api.agent_routes import router as agent_router

# 在 include_router 区域添加（先不加，等 routes.py 删除重复后）
```

**Step 3: 逐领域迁移**

每个领域的迁移流程：
1. 从 routes.py 中**注释掉**该领域的所有端点
2. 在 main.py 中注册对应的 domain router
3. 运行全量测试 → 确认无路由冲突
4. 如果有冲突：检查路径是否与已有 domain router 重复
5. 确认通过后，**删除** routes.py 中的注释代码

执行顺序（由大到小，降低批量风险）：
1. DPABI (20 端点) → main.py 注册 dpabi_router
2. rs-fMRI (25 端点) → main.py 注册 rsfmri_router
3. Agent/Run/Retry/Scheduler (11 端点) → main.py 注册 agent_router
4. GPU (4 端点) → 新建 gpu_routes.py
5. Session/History (6 端点) → 新建 session_routes.py
6. Advisor (5 端点) → 新建 advisor_routes.py
7. Experiments (6 端点) → 新建 experiment_routes.py
8. Artifacts/Insights (3 端点) → 新建 artifact_routes.py
9. Real Data/Sandbox/Workflow (6 端点) → 新建 realdata_routes.py

**Step 4: 收尾**
- routes.py 只保留 `/health` 和 `/api/project-config` 两个基础端点
- 剩余的 Files/Logs/Bundle/Docs/Deploy/Release 端点（~8 个）合并到 pipeline_routes.py 或在 routes.py 中保留

### 风险与缓解

| 风险 | 缓解 |
|------|------|
| 路由冲突 (同一路径注册两次) | 激活 domain router 前先从 routes.py 删除对应端点 |
| domain router 中的端点行为不一致 | 对比两端代码，确认业务逻辑一致后再激活 |
| 新建 router 的路径与已有路径冲突 | FastAPI 会在启动时检测重复路由并报错 |

### 验收标准

- routes.py 从 1609 行减少到 < 100 行
- 全量测试通过
- `/api/v1/` 版本中间件对所有 domain router 生效
- 前端调用不受影响（路径不变）

---

## Sprint 3: node_registry 插件化

**目标**: 将 node_registry.py (1361 行, 180+ 行 import, 85+ 节点) 按领域拆分为 7 个文件

### node_id 领域分类

基于 import 来源和业务语义分析：

| 领域 | 节点数（约） | 目标文件 | 示例 node_id |
|------|------------|---------|-------------|
| Core | ~8 | core_nodes.py | environment_check, spm_smoke_test, create_synthetic_bids, data_inspection, subject_qc, dataset_evaluation, contract_smoke |
| SPM | ~12 | spm_nodes.py | spm_smooth_subject, spm_realign_subject, motion_qc_*, spm_slice_timing_subject, spm_coregister_subject, registration_qc_*, spm_segment_subject, tissue_qc_*, spm_normalize_subject, normalization_qc_*, spm_smooth_subject, smoothing_qc_* |
| DPABI | ~18 | dpabi_nodes.py | dpabi_capability_inspection, dpabi_wrapper_scaffold, dpabi_input_manifest, dpabi_preflight, dpabi_run_plan, dpabi_sandbox_smoke_run, dpabi_signature_probe, dpabi_wrapper_contracts, dpabi_single_function_sandbox, dpabi_subject_smooth, dpabi_subject_wrapper_report, dpabi_wrapper_validation_matrix, dpabi_template_library, dpabi_template_instantiate, dpabi_template_execute, dpabi_nuisance_regression_contract, dpabi_temporal_filtering_contract, dpabi_alff_falff_contract, dpabi_reho_contract, dpabi_fc_contract |
| GPU | ~5 | gpu_nodes.py | gpu_alff_subject, gpu_reho_subject, gpu_nuisance_regression_subject, gpu_temporal_filtering_subject, gpu_functional_connectivity_subject |
| rs-fMRI | ~14 | rsfmri_nodes.py | rsfmri_preprocessing_plan, nuisance_regression_subject, nuisance_regression_report, temporal_filtering_subject, temporal_filtering_report, alff_falff_subject, alff_falff_report, alff_falff_gpu_contract, alff_falff_dpabi_contract, reho_subject, reho_report, reho_gpu_contract, reho_dpabi_contract, fc_subject, fc_report, fc_gpu_contract, fc_dpabi_contract |
| QC | ~13 | qc_nodes.py | motion_qc_subject, motion_qc_dataset_report, slice_timing_dataset_report, st_realign_motion_chain_report, registration_qc_dataset_report, tissue_qc_dataset_report, normalization_qc_dataset_report, smoothing_qc_dataset_report, nuisance_regression_dataset_report, temporal_filtering_dataset_report, alff_falff_dataset_report, reho_dataset_report, fc_dataset_report |
| Report | ~8 | report_nodes.py | report_export, report_validate, release_readiness, docs_inventory, group_dataset_summary, dpabi_subject_wrapper_report |

### 目标文件结构

```
src/backend/app/runtime/
├── node_registry.py               # [重构] 轻量 NodeRegistry 类 + NODE_REGISTRY 全局实例
├── node_registry/
│   ├── __init__.py                # [新建] create_registry() 工厂函数
│   ├── base.py                    # [新建] NodeRunner Protocol
│   ├── core_nodes.py              # [新建] ~8 核心/环境/smoke 节点
│   ├── spm_nodes.py               # [新建] ~12 SPM 预处理节点
│   ├── dpabi_nodes.py             # [新建] ~18 DPABI 节点
│   ├── gpu_nodes.py               # [新建] ~5 GPU 计算节点
│   ├── rsfmri_nodes.py            # [新建] ~14 rs-fMRI 处理节点
│   ├── qc_nodes.py                # [新建] ~13 QC 节点
│   └── report_nodes.py            # [新建] ~8 报告/导出节点
```

### NodeRunner Protocol

```python
# runtime/node_registry/base.py [新建]
from typing import Any, Protocol

class NodeRunner(Protocol):
    @property
    def node_id(self) -> str: ...

    def __call__(self, node: Any, project_config: dict, context: dict) -> dict[str, Any]:
        """Execute the node and return result dict with 'ok', 'outputs', etc."""
        ...

class NodeRegistry:
    def __init__(self):
        self._runners: dict[str, NodeRunner] = {}

    def register(self, runner: NodeRunner) -> None:
        if runner.node_id in self._runners:
            raise ValueError(f"Duplicate node_id: {runner.node_id}")
        self._runners[runner.node_id] = runner

    def get(self, node_id: str) -> NodeRunner:
        return self._runners[node_id]

    def list_nodes(self) -> list[str]:
        return sorted(self._runners.keys())
```

### 向后兼容方案

**关键**: `node_registry.py` 顶部的全局 `NODE_REGISTRY` dict 被 `pipeline_executor.py` 直接引用。必须保持兼容。

```python
# runtime/node_registry.py [重构后]
from src.backend.app.runtime.node_registry.create import create_registry

NODE_REGISTRY = create_registry()  # 返回 NodeRegistry 实例

# 为向后兼容，支持 dict 式访问
NODE_REGISTRY.__getitem__ = lambda self, key: self.get(key)
NODE_REGISTRY.keys = lambda self: self.list_nodes()
```

### 执行步骤

1. **拍节点快照**: 遍历当前 NODE_REGISTRY，输出所有 `(node_id, module.function)` 映射
2. **创建 base.py + NodeRegistry 类**
3. **为 7 个领域创建文件**，分配 node_ids
4. **逐文件迁移**：复制 runner 函数定义 + NodeRegistry.register() 调用
5. **更新 node_registry.py**：替换 1361 行为 `from .node_registry.create import create_registry; NODE_REGISTRY = create_registry()`
6. **全量测试**: 关键回归 — DAG 调度、流水线执行、所有 80+ 节点的 node_id 不变

### 风险与缓解

| 风险 | 影响 | 缓解 |
|------|------|------|
| node_id 字符串变化 | DAG 调度中断 | 拆分前拍快照，拆分后 diff 验证 |
| runner 函数签名变化 | pipeline_executor 调用报错 | 保持签名不变，只改 import 路径 |
| import 路径变更 | 下游引用报错 | 旧路径保留兼容 import（`from src.backend.app.runtime.node_registry import NODE_REGISTRY` 不变） |

### 验收标准

- node_registry.py 从 1361 行减少到 < 30 行
- 所有 node_id 字符串与拆分前完全一致
- 全量测试通过（包括流水线端到端测试）
- NODE_REGISTRY 全局变量仍然可用（向后兼容）

---

## 风险评估矩阵

| 风险 | Sprint | 概率 | 影响 | 缓解措施 |
|------|--------|------|------|---------|
| 错误处理迁移引入新的异常类型导致下游崩溃 | S1 | 低 | 中 | 在 _shared.py 中先验证，逐文件测试 |
| Domain router 激活后与 routes.py 路由冲突 | S2 | 中 | 高 | FastAPI 启动时检测重复路由，先注释后删除 |
| Domain router 端点行为不一致 | S2 | 低 | 中 | 逐端点对比代码，优先用已有测试覆盖 |
| node_id 字符串丢失或变化 | S3 | 中 | 高 | 快照 diff，自动化验证 |
| import 循环引用 | S3 | 低 | 中 | 保持旧的 import 路径可用 |

---

## 时间线

```
Sprint 1: ████████████░░░░░░░░░░░░  ~1 天
Sprint 2: ░░░░░░░░░░░░████████████  ~2 天
Sprint 3: ░░░░░░░░░░░░░░░░░░░░░░░░  ~2-3 天
                                    (后两个 Sprint 需更多审查)
```
