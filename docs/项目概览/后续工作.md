# Next Actions

The authoritative priorities are maintained in
[`PROJECT_STATE.md`](../../PROJECT_STATE.md#next-work). This file is a short,
stable pointer for older documents that link to `docs/项目概览/后续工作.md`.

## Current Priorities

1. Keep public DICOM conversion default-blocked unless a maintainer explicitly
   approves release enablement.
2. Continue real-project validation of the reviewed native preprocessing path,
   including subject-level QC, atlas-grounded ROI time series, FC artifacts,
   report export, and package validation.
3. Keep capability labels evidence-based: distinguish unavailable,
   metadata-only, computed, partial, and validated states.
4. Continue splitting legacy backend and frontend modules only through focused,
   tested changes.
5. Validate packaged desktop workflows on Windows without weakening rawdata,
   Approval Gate, audit, or safe-path protections.

Completed milestone logs are intentionally not copied here. Durable historical
outcomes belong in release notes, ADRs, or `specs/阶段记录/`.
