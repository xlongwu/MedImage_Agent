# 开发日志 — 2026-05-29 M6-T001

> SPM / DPABI execution safety review

## 目标

对 SPM/DPABI 节点做全面安全审计，为逐步开放提供分阶段路线。
不开放任何 SPM/DPABI subject-level execution。

## 审计覆盖

| 维度 | 覆盖 |
|------|------|
| SPM 节点 | 7 个 (spm_smoke_test + 6 subject-level) |
| DPABI 节点 | 20 个 (15 contract + 2 catalog-only + 3 execution) |
| Path safety | rawdata readonly, output dirs, path traversal |
| MATLAB command safety | subprocess.run, shell injection, quoting |
| Approval granularity | plan-level, node-level, backend-level, wildcard |
| GUI/manual_required | 策略不变 — 继续阻断 |

## 关键发现

1. **spm_smoke_test** 可最先开放 — synthetic data, medium risk
2. **DPABI contract/capability 节点** 可开放 — Python-only, 无 MATLAB
3. **wildcard approval `["*"]` 绝不可覆盖 MATLAB 节点**
4. **2 个 DPABI 节点缺少 runner 注册** — 无法在当前系统运行
5. **MATLAB 调用使用 `subprocess.run` list form + `_matlab_quote()`** — 无 shell injection
6. **`matlab_command` 可配置风险** — 建议白名单

## 新增文件

| 文件 | 说明 |
|------|------|
| `docs/SPM_DPABI_SAFETY_REVIEW.md` | 全面安全审计 + 8-phase 开放路线 |

## 更新文件

| 文件 | 说明 |
|------|------|
| `docs/CURRENT_STATE.md` | M6-T001 ✅ |
| `docs/NEXT_ACTIONS.md` | M6-T002 recommended |
| `docs/TASK_BACKLOG.md` | M6-T001 status |

## 测试

```text
pytest --tb=short                   →  550 passed, 4 skipped
npm --prefix src/frontend run build →  ✅
```

## 是否修改代码

**否。** 仅文档。
