# 开发日志 — 2026-05-29 M6-T005a

> spm_realign_subject safety contract + sandbox design

## 目标

对 `spm_realign_subject` 做执行前安全契约审计和 sandbox 设计。
不开放真实执行。

## 新增文件

| 文件 | 说明 |
|------|------|
| `docs/SPM_REALIGN_SAFETY_CONTRACT.md` | 完整安全契约 (9 章节) |

## 审计发现

### Runner contract
- 位置: `tools/spm_realign_runner.py` → `run_spm_realign_subject()`
- 9 个参数: matlab_command, spm_dir, subject_id, input_bold, derivatives_dir, work_dir, log_dir, approved, allow_derivative_input

### Path safety
- ✅ 输入安全检查: synthetic/derivatives only
- ✅ 不写 rawdata
- ✅ `subprocess.run(list)` + `_matlab_quote()`
- ⚠️ 缺少 `validate_matlab_runtime_config()` preflight

### Approval requirements
- ✅ M6-T003 已要求 explicit node + backend
- ✅ wildcard 被阻断
- ⚠️ 缺少 subject-level sandbox flag

## Sandbox rollout plan (4 phases)
```
Phase 1: Safety contract (M6-T005a) ✅
Phase 2: MATLAB safety preflight (M6-T005b)
Phase 3: Sandbox execution (M6-T005c)
Phase 4: Reviewed execution allowlist (M6-T005d)
```

## 测试
```text
pytest --tb=short  →  616 passed, 4 skipped (无变化，未修改代码)
```
