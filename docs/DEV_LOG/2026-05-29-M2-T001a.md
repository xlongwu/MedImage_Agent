# 开发日志 — 2026-05-29 (M2-T001a)

## 任务：M2-T001a — Tool Catalog 只读 MVP

### 新增

#### src/backend/app/runtime/tool_catalog.py

- `ToolCatalogItem` dataclass（11 个字段）
- `TOOL_METADATA`：~30 个核心 node 的精确元数据
- `_fallback()`：基于 id 前缀推导 metadata（覆盖 34 个未定义 node）
- `build_tool_catalog()`：遍历 NODE_REGISTRY → 每个 node 生成一个 item
- `get_tool_catalog_item()`：按 id 查询单个 node
- `catalog_as_dicts()`：JSON 可序列化的 dict 列表

#### tests/unit/test_tool_catalog.py

13 个测试：覆盖完整性、字段存在性、特定 node 风险标记、fallback 行为、序列化、无副作用

#### docs/TOOL_CATALOG.md

Tool Catalog 目的、与 Node Registry 的区别、字段定义、风险规则、LLM Planner 使用方式、当前限制

### 覆盖范围

| 类别 | 数量 |
|------|:---:|
| 明确定义 metadata | ~30 |
| Fallback（前缀推导） | ~34 |
| **总计（=NODE_REGISTRY keys）** | **64** |

### Fallback 规则

| 前缀/模式 | backend | risk | approval |
|-----------|---------|:---:|:---:|
| `spm_*` | matlab-spm | high | True |
| `dpabi_*` (contract 类) | python | low | False |
| `dpabi_*` (sandbox/single_function) | matlab | high | True |
| `gpu_*` | gpu | medium | False |
| `*_contract` / `*_candidate_contract` | unknown | low | False |
| `*_qc_*` / `*_dataset_report` | unknown | low | False |
| `gui_*` | unknown | high | True |

### 测试结果

```
pytest tests/unit/test_tool_catalog.py -v    13 passed
pytest --tb=short                            255 passed, 4 skipped
```

### 未修改

- node_registry / pipeline_executor / agent_plan / agent_runtime / routes — 零改动
- tools / frontend / examples / CI workflow — 零改动
