# 开发日志 — 2026-05-29 M6-T005b-fix

> SPM-only validator, remove dummy dpabi_dir from realign preflight

## 问题

`run_spm_realign_subject()` 使用 `validate_matlab_runtime_config()` 时传了 dummy `dpabi_dir="./third_party/DPABI"`，SPM-only 节点不应依赖 DPABI path。

## 修改

| 文件 | 操作 | 说明 |
|------|:---:|------|
| `src/backend/app/safety/matlab_safety.py` | ✏️ 修改 | +`validate_spm_runtime_config()` |
| `src/backend/app/tools/spm_realign_runner.py` | ✏️ 修改 | 替换为 SPM-only validator |
| `tests/unit/test_matlab_safety.py` | ✏️ 更新 | 29→35 (+6) |
| `docs/CURRENT_STATE.md` | ✏️ 更新 | M6-T005b-fix ✅, 634 passed |

## 新增函数

```python
def validate_spm_runtime_config(
    *, matlab_command: str, spm_dir: str | Path
) -> MatlabSafetyResult
```

- 复用 `validate_matlab_command()` + `validate_third_party_dir(spm_dir)`
- 不校验 `dpabi_dir`
- `validate_matlab_runtime_config()` 保持不变

## 测试

```text
tests/unit/test_matlab_safety.py     →  35 passed (+6)
tests/unit/test_spm_realign_safety.py →  12 passed
pytest --tb=short                      →  634 passed, 4 skipped
```
