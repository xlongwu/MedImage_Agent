# 开发日志 — 2026-05-29 (M2-T001b)

## 任务：M2-T001b — Tool Catalog API endpoint

### 新增

#### src/backend/app/api/tool_catalog_routes.py

两个只读 endpoint：
- `GET /api/tools/catalog` — 完整工具目录
- `GET /api/tools/catalog/{node_id}` — 单个工具（不存在→404）

#### src/backend/app/main.py

新增 `include_router(tool_catalog_router)`

#### tests/unit/test_tool_catalog_api.py

12 个测试：200/ok/count/字段/序列化/404/无副作用

### 响应结构

```
GET /api/tools/catalog → {"ok": true, "count": 64, "items": [...]}
GET /api/tools/catalog/{node_id} → {"ok": true, "item": {...}}
不存在 node → 404 {"detail": "Tool catalog item not found: ..."}
```

### 测试结果

```
tests/unit/test_tool_catalog_api.py    12 passed
tests/unit/test_tool_catalog.py        13 passed
pytest --tb=short                     267 passed, 4 skipped
```

### 未修改

- node_registry / pipeline_executor / agent_plan / agent_runtime — 零改动
- tool_catalog.py — 零改动
- tools / frontend / examples / CI — 零改动
