# 开发日志 — 2026-05-29 M5-T018b

> synthetic-only end-to-end reviewed execution smoke

## 目标

验证 backend safe execution 路径在 synthetic-only / Python-only 场景下
端到端可行性分析 + 增强 smoke 文档 + 新增专用 example config。

不新增 CI integration test（CI 稳定性未验证）。

## 分析结论

### 可行性判断

| 问题 | 结论 |
|------|------|
| safe allowlist 可用节点 | `create_synthetic_bids`, `data_inspection` 均为 Python-only |
| `create_synthetic_bids` 可执行 | ✅ 已注册, 需要 `numpy`+`nibabel` |
| `data_inspection` 可执行 | ✅ 可在 synthetic 数据上运行 |
| 现有 pipeline YAML | ❌ 无 synthetic-only pipeline |
| 现有 project config | ❌ 要求 SPM/DPABI 路径 |
| CI 适合性 | ❌ 当前不适合 (numpy/nibabel + state files) |

### 决策

1. **增强手动 smoke 文档** — 可复现步骤 + 故障排查
2. **新增 `examples/project_config_synthetic_smoke.yaml`** — 专用 config
3. **不新增 CI integration test** — 推迟到 M5-T018c

## 修改文件

| 文件 | 操作 | 说明 |
|------|:---:|------|
| `docs/REVIEWED_EXECUTION_SMOKE_TEST.md` | ✏️ 重写 | 精确可复现步骤 + 验证清单 + 故障排查 |
| `examples/project_config_synthetic_smoke.yaml` | 🆕 新建 | 专用 smoke project config |
| `docs/CURRENT_STATE.md` | ✏️ 更新 | M5-T018b ✅ |
| `docs/DEV_LOG/2026-05-29-M5-T018b.md` | 🆕 新建 | 本日志 |

## 未修改文件

核心代码未修改（`execute_reviewed_routes.py`, `pipeline_executor.py`, etc.）。

## 新增 example 说明

`examples/project_config_synthetic_smoke.yaml`:
- `spm_dir`/`dpabi_dir` 为 dummy 路径（通过 ProjectSettings 校验但不实际使用）
- `rawdata_readonly: true`
- 仅用于 Python-only smoke nodes

## 测试

```text
pytest --tb=short  # 547 passed, 4 skipped (无变化，未修改测试)
```
