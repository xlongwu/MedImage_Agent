# 开发日志 — 2026-05-29 M6-T005b

> spm_realign_subject MATLAB safety preflight integration

## 目标

在 `run_spm_realign_subject()` 中接入 `validate_matlab_runtime_config()` preflight。
safety errors 阻断，warnings 通过。

## 修改文件

| 文件 | 操作 | 说明 |
|------|:---:|------|
| `src/backend/app/tools/spm_realign_runner.py` | ✏️ 修改 | +safety preflight before input check |
| `tests/unit/test_spm_fake_matlab_contract.py` | ✏️ 修改 | matlab_command: "fake-matlab" → "matlab" |
| `tests/unit/test_spm_realign_safety.py` | 🆕 新建 | 12 tests |
| `docs/CURRENT_STATE.md` | ✏️ 更新 | M6-T005b ✅, 628 passed |

## 实现

### Preflight 接入位置

在 `approved=True` 检查后、`_is_safe_synthetic_input()` 前:

```python
safety_result = validate_matlab_runtime_config(
    matlab_command=matlab_command,
    spm_dir=spm_dir,
    dpabi_dir="./third_party/DPABI",
)
if not safety_result.ok:
    return { "ok": False, "stage": "matlab_safety_preflight", ... }
```

### Error 行为

- `ok=False`, `stage="matlab_safety_preflight"`
- `matlab_called=False`, `spm_called=False`
- 包含 `safety.to_dict()` in result

### Warning 行为

- 不阻断 (warnings only)
- 继续原有流程

## 测试

```text
tests/unit/test_spm_realign_safety.py  →  12 passed
tests/unit/test_spm_fake_matlab_contract.py → 18 passed (1 fix)
pytest --tb=short                        →  628 passed, 4 skipped
```

## 下一步

M6-T005c: spm_realign sandbox execution (allowlist + reviewed execution)
