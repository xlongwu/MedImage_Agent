# M9-GUI-GUARD-T002: Provider Policy Gate Implementation

**Date:** 2026-07-11

**Status:** COMPLETE

## Summary

Implemented the first layer of the 14-layer GUI Agent API guard pipeline: the provider policy gate. This is the first M9 task that modifies production code to close the known `/api/gui-agent/*` bypass gap.

## Production Code Changes (3 files)

### NEW: `src/backend/app/runtime/gui_agent_guard.py`

- `GuiGuardResult` dataclass — 12 fields including 6 safety flags
- `validate_gui_provider_policy()` — pure function, no side effects
- Only `provider="mock"` returns `ok=true`
- 5 blocked provider categories: pywinauto, real, desktop, browser, manual
- Case-insensitive, whitespace-tolerant normalization
- `approved=true`, `real_provider_feature_enabled`, `allow_real_provider` all ignored in T002
- CI mode blocks all non-mock unconditionally

### MODIFIED: `src/backend/app/api/gui_agent_routes.py`

- Added `_guard_provider()` helper — calls `validate_gui_provider_policy()` and raises HTTP 403 if blocked
- Integrated at `POST /api/gui-agent/sessions` — validates provider before session creation
- Added `ValueError` catch in step/screenshot routes for runtime-level defense
- Error response: HTTP 403 with structured `GuiGuardResult.to_dict()` as detail

### MODIFIED: `src/backend/app/runtime/gui_agent.py`

- Rewrote `_provider()` function — calls `validate_gui_provider_policy()` before any provider construction
- PyWinAuto path is now unreachable (guard blocks it before the `if name == "pywinauto"` branch)
- Raises `ValueError` with `GuiGuardResult.to_dict()` on blocked providers

## Test File (NEW)

`tests/unit/test_gui_agent_provider_policy_gate.py` — 43 tests:

- **A. Pure Function Tests (25):** mock allowed, None/empty/whitespace blocked, pywinauto/real/desktop/browser/manual blocked, unknown blocked, case normalization, approved=true no bypass, feature flags no bypass, CI mode, safety flags, JSON serializable
- **B. API Route Tests (9):** mock session creation works, pywinauto/real/desktop/unknown blocked (HTTP 403), approved=true + pywinauto blocked, mock step works, runtime-level defense, screenshot route blocked
- **C. Regression Tests (6):** GUI reviewed execution still blocked, executor_called=false, SPM/DPABI/GPU allowlists unchanged
- **D. Safety Tests (3):** Mock provider no desktop, no pywinauto import, all flags combined no bypass

## Test Results

- New tests: 43/43 passed
- Full suite: 1235 passed, 4 skipped
- Frontend build: passed
- GUI reviewed execution blocklist (T004): 38/38 still pass
- SPM/DPABI/GPU regression: 0 failures
