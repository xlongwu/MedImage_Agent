# 开发日志 — 2026-05-29 M5-T018c

> CI-safe reviewed execution integration smoke with mocked executor

## 目标

在 CI 中新增 integration test，验证 `/api/plans/execute-reviewed` 的
完整 route 链路走到 mocked executor，不运行真实工具。

## 修改文件

| 文件 | 操作 | 说明 |
|------|:---:|------|
| `tests/integration/test_reviewed_execution_synthetic_smoke.py` | 🆕 新建 | 3 CI-safe integration tests |
| `docs/CURRENT_STATE.md` | ✏️ 更新 | M5-T018c ✅, 550 passed |

## 新增测试 (3 个)

### test 1: `test_safe_plan_execution_submitted`
- safe Python-only plan (`data_inspection`)
- 18 assertions: HTTP 200, EXECUTION_SUBMITTED, executor_called=true,
  submitted=true, run_id, executor called once, correct args, YAML exists
  + parseable + has fields, audit persisted + file exists, no rawdata,
  JSON serializable

### test 2: `test_unsafe_spm_plan_blocked`
- SPM plan → EXECUTION_POLICY_BLOCKED
- executor NEVER called (len(calls)==0)

### test 3: `test_dry_run_true_does_not_call_executor`
- dry_run=true → DRY_RUN_OK
- executor NOT called

## mocked executor 策略

```python
def _fake_run_pipeline(project_config_path, pipeline_path):
    executor_calls.append({...})
    return {"status": "SUCCESS", "run_id": "ci-mock-run-001"}

monkeypatch.setattr(
    "src.backend.app.api.execute_reviewed_routes.run_pipeline",
    _fake_run_pipeline,
)
```

## project_config 构造

动态生成 `tmp_path / "project_config.yaml"`:
- `spm_dir`/`dpabi_dir` 为 tmp_path 下 dummy 路径（通过 ProjectSettings 校验）
- `rawdata_readonly: true`

## 测试结果

```text
tests/integration/test_reviewed_execution_synthetic_smoke.py  →  3 passed
pytest --tb=short                                              →  550 passed, 4 skipped
```

## CI 兼容性

- ✅ 不依赖 MATLAB/SPM/DPABI/GPU
- ✅ 不依赖真实数据
- ✅ 所有文件写入 tmp_path
- ✅ executor mocked
- ✅ CI-stable
