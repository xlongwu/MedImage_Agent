# 开发日志 — 2026-05-29 (M1-T005b)

## 任务：M1-T005b — pipeline_executor 接入 ProjectSettings 校验

### 背景

M1-T005a 已将 agent_plan 的 `_load_project_config()` 接入 ProjectSettings 校验。
M1-T005b 是对称操作：在 `pipeline_executor.py` 的 `load_project_config()` 中
以完全相同的方式接入。

### 修改

#### src/backend/app/runtime/pipeline_executor.py

`load_project_config()` 中新增两行（与 M1-T005a 对称）：

```python
from src.backend.app.config import ProjectSettings
ProjectSettings.from_yaml(path)  # 关键字段缺失 → ValueError
```

返回类型不变（`dict[str, Any]`）。`run_pipeline()` 中已有的 try/except 天然兼容：

```python
try:
    project_config = load_project_config(project_config_path)
except Exception as exc:
    return {"status": "INVALID", ...}  # ← ValueError 被正确捕获
```

### 测试

`tests/unit/test_pipeline_executor_project_settings.py` — 10 个测试：

| # | 测试 | 验证 |
|---|------|------|
| 1 | test_load_project_config_returns_dict | 仍返回 dict |
| 2 | test_missing_work_dir_raises_value_error | ValueError |
| 3 | test_missing_spm_dir_raises_value_error | ValueError |
| 4 | test_file_not_found_raises | FileNotFoundError |
| 5 | test_run_pipeline_returns_invalid_on_missing_work_dir | bad config → INVALID |
| 6 | test_run_pipeline_returns_invalid_on_missing_spm_dir | bad config → INVALID |
| 7 | test_run_pipeline_returns_invalid_on_nonexistent_config | 不存在文件 → INVALID |
| 8 | test_no_real_matlab_spm_dpabi_executed | bad config → 提前返回，不创建 pipeline_runs/ |
| 9 | test_bad_config_writes_summary_in_work_dir | INVALID 时 error 字段存在 |
| 10 | test_valid_config_with_nonexistent_node | 合法 config + 不存在的 node → FAILED（非 INVALID） |

所有测试使用 tmp_path，运行后自动清理。

### 测试结果

```
pytest tests/unit/test_pipeline_executor_project_settings.py -v   10 passed
pytest tests/unit/test_agent_plan_project_settings.py -v           12 passed
pytest tests/unit/test_project_settings.py -v                      14 passed
pytest tests/unit/test_release_readiness.py -v                     24 passed
pytest (全量)                                                     234 passed, 4 skipped
```

### M1 ProjectSettings 迁移完成总结

| 入口点 | 位置 | 状态 |
|--------|------|:---:|
| agent_plan._load_project_config() | PLAN 阶段 | ✅ T-005a |
| pipeline_executor.load_project_config() | EXECUTE 阶段 | ✅ T-005b |
| routes._load_project_config() | API 层 | 🔲 待后续 |

两个核心执行路径（PLAN + EXECUTE）均已完成配置校验接入。

### 未触碰的业务代码

- run_pipeline() 主体逻辑 — 零改动
- scheduler、node_registry、state_store — 零改动
- agent_runtime、agent_plan（除 M1-T005a 外）— 零改动
- routes、tools、frontend — 零改动
- examples/*.yaml — 零改动
