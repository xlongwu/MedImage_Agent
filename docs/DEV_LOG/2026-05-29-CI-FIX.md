# 开发日志 — 2026-05-29 (CI-FIX)

## 任务：CI 修复 — version.py 未跟踪 + package-lock 不同步

### Backend 修复

**根因**：`src/backend/app/version.py` 存在于本地但未被 git 跟踪。CI clean checkout 缺少该文件 → `routes.py` import 失败 → 整个测试收集中断。

**修复**：`git add src/backend/app/version.py`。

### Frontend 修复

**根因**：`package-lock.json` 缺少 `devDependencies`（electron、electron-builder 及其传递依赖）。可能是初始生成时使用了 `--production` 或 npm 版本不兼容导致。

**修复**：运行 `npm install --package-lock-only` 重新生成完整 lockfile（新增 4421 行，包含所有 devDependencies 解析）。

### 测试结果

```
pytest: 242 passed, 4 skipped
npm ci: 需要网络（sandbox 限制），CI 环境可正常运行
npm run build: sandbox node_modules 被清空，需 CI 验证
```

### 未修改的文件

- CI workflow 零改动
- 业务逻辑零改动
