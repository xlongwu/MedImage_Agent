# 开发日志 — 2026-05-29 M6-T004b

> spm_smoke_test reviewed execution allowlist integration

## 目标

将 `spm_smoke_test` 纳入 reviewed execution 受控 allowlist，
作为唯一允许的 SPM reviewed execution 节点。

## 修改文件

| 文件 | 操作 | 说明 |
|------|:---:|------|
| `src/backend/app/planner/plan_adapter.py` | ✏️ 修改 | +allowed_spm_smoke_nodes, spm_smoke_test exception |
| `src/backend/app/api/execute_reviewed_routes.py` | ✏️ 修改 | safe allowlist 支持 spm_smoke_test |
| `tests/unit/test_plan_adapter.py` | ✏️ 更新 | 20→23 (+3 M6-T004b) |
| `tests/unit/test_execute_reviewed_api.py` | ✏️ 更新 | 100→105 (+5 M6-T004b) |
| `docs/CURRENT_STATE.md` | ✏️ 更新 | M6-T004b ✅, 616 passed |

## Policy 变化

### classify_plan_nodes()

```
新增: allowed_spm_smoke_nodes — spm_smoke_test only
保留: blocked_spm_nodes — all other spm_* nodes
```

### _check_safe_allowlist()

```
允许: allowed_python_nodes + allowed_spm_smoke_nodes
阻断: GPU, contract (不变)
```

## Allowlist

| 节点 | 状态 |
|------|:---:|
| `spm_smoke_test` | ✅ **新允许** |
| `spm_realign_subject` | ❌ 阻断 |
| `spm_slice_timing_subject` | ❌ 阻断 |
| `spm_coregister_subject` | ❌ 阻断 |
| `spm_segment_subject` | ❌ 阻断 |
| `spm_normalize_subject` | ❌ 阻断 |
| `spm_smooth_subject` | ❌ 阻断 |

## Approval 要求

`spm_smoke_test` 必须:
- `approved_nodes` 显式包含 `"spm_smoke_test"`
- `approved_backends` 包含 `"matlab-spm"`
- wildcard `["*"]` 不覆盖

## 测试

```text
test_plan_adapter.py       →  23 passed (+3)
test_execute_reviewed_api.py →  105 passed (+5)
pytest --tb=short           →  616 passed, 4 skipped
```

## 下一步

M6-T005: `spm_realign_subject` subject-level reviewed execution (需独立 sandbox)
