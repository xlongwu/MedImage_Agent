# 开发日志 — 2026-05-29

## 任务：M1-T002 — 修复 release readiness 与当前项目结构的一致性

### 背景

在前一阶段（2025-07-18），M1 的文档建设工作已完成（AGENTS.md、CLAUDE.md、PROJECT_GOAL.md、ARCHITECTURE.md、ROADMAP.md、TASK_BACKLOG.md、SAFETY_BOUNDARIES.md、DEVELOPMENT_WORKFLOW.md、DECISIONS/0001、DECISIONS/0002）。README.md 和 developer_guide.md 中的旧路径已被修正。

但 `release_readiness.py` 仍有一个 bug：
- API endpoint 检查使用 `/api/rsfmri/report-validator`，而实际端点是 `/api/rsfmri/report-validation`

此外，release_readiness 缺少对 M1 新建文档的检查，也没有对 `outputs/exports/rsfmri_report_package/` 不存在的情况做优雅处理（fresh clone 环境会 FAIL）。

### 修改内容

#### 1. src/backend/app/tools/release_readiness.py

**Bug 修复**：
- `/api/rsfmri/report-validator` → `/api/rsfmri/report-validation`

**新增检查**：
- 项目结构：`src/backend/app/schemas`、`src/backend/app/advisor`、`docs/DECISIONS`
- 项目结构：`AGENTS.md` 文件存在性
- 新增 `docs_m1` 类别：检查 10 个 M1 文档交付物
- AGENTS.md 内容检查：包含 "rawdata"、"approval gate"、"禁止"、"LLM"
- ARCHITECTURE.md 行数检查：≥ 100 行
- README.md 启动命令检查：使用 `uvicorn src.backend.app.main:app` 和 `cd src/frontend`

**WARNING 替代 FAIL**：
- 新增 `warn()` 函数：记录 WARNING 但不标记 FAIL
- `outputs/exports/rsfmri_report_package/` 不存在时使用 WARNING 而非 FAIL
- 修复了 case-insensitive 搜索的 bug（`kw.lower() in text.lower()`）

#### 2. tests/unit/test_release_readiness.py

完全重写，从 1 个测试函数扩展到 24 个测试函数：

| 类别 | 测试数 | 说明 |
|------|--------|------|
| 结构 | 2 | result 包含预期 key，checks 非空 |
| 路径正确性 | 6 | src/backend、src/frontend、schemas、advisor、无旧路径 |
| Bug 修复验证 | 1 | /api/rsfmri/report-validation 正确 |
| M1 文档 | 10 | 10 个文档存在检查 + 计数 |
| AGENTS.md 内容 | 4 | rawdata、approval gate、禁止、LLM |
| ARCHITECTURE.md | 1 | 行数 ≥ 100 |
| README.md | 2 | 启动命令正确 |
| 回归 | 3 | import diagnostics 相关检查 |
| 输出文件 | 2 | 4 个输出文件存在且有效 |
| WARNING 策略 | 1 | report_package 缺失时不为 FAIL |

#### 3. docs/CURRENT_STATE.md（新建）

记录当前里程碑进度、交付物状态、release_readiness 状态、已知问题。

#### 4. docs/NEXT_ACTIONS.md（新建）

列出立即执行（P0）、短期执行（P1）、持续执行的任务。

#### 5. docs/DEV_LOG/2026-05-29.md（本文档）

#### 6. docs/TASK_BACKLOG.md（更新）

标记 T-0002 状态变更。

### 测试结果

```
pytest tests/unit/test_release_readiness.py -v
24 passed in 0.11s
```

release_readiness 当前状态：
- 状态：PASS
- 78 checks total, 78 PASS, 0 FAIL, 0 WARNING

### 风险

1. release_readiness 在 fresh clone 环境（无 outputs/exports/ 目录）下仍能 PASS，因为 report_package 缺失改用 WARNING
2. 全量 `pytest` 尚未运行，可能存在其他测试失败
3. 前后端启动尚未验证

### 下一步

- T-0003：运行全量 pytest
- T-0004：验证前后端启动
- T-0005：audit_logger 决策
