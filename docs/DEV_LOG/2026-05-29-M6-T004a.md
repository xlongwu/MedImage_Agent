# 开发日志 — 2026-05-29 M6-T004a

> SPM smoke test safety preflight integration

## 目标

将 `matlab_safety.py` 接入 `spm_smoke_test` 的 preflight 路径。

## 修改文件

| 文件 | 操作 | 说明 |
|------|:---:|------|
| `src/backend/app/tools/spm_runner.py` | ✏️ 修改 | 新增 `spm_smoke_preflight()` |
| `tests/unit/test_spm_smoke_safety.py` | 🆕 新建 | 15 tests |
| `docs/CURRENT_STATE.md` | ✏️ 更新 | M6-T004a ✅, 608 passed |

## 实现

### spm_smoke_preflight()

```python
def spm_smoke_preflight(*, matlab_command, spm_dir, dpabi_dir=None) -> dict
```

- 调用 `validate_matlab_runtime_config()`
- errors → `ok=False`, `would_call_matlab=False`
- warnings → advisory only, `ok=True`
- 不调用 MATLAB/subprocess

### 返回结构

```json
{
  "ok": true,
  "safety": {"ok": true, "errors": [], "warnings": [...]},
  "matlab_command_checked": true,
  "spm_dir_checked": true,
  "dpabi_dir_checked": true,
  "would_call_matlab": true,
  "notes": ["SPM smoke test preflight completed.", "No MATLAB was called during preflight."]
}
```

## 测试

```text
tests/unit/test_spm_smoke_safety.py  →  15 passed
pytest --tb=short                      →  608 passed, 4 skipped
```

## 安全

- ✅ 不调用 MATLAB/SPM
- ✅ 不修改 allowlist
- ✅ 不修改 Approval Gate
- ✅ 不写 rawdata

## 下一步

M6-T004b: 将 `spm_smoke_test` 加入 reviewed execution safe allowlist
