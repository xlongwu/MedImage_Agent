# 任务：基于当前项目状态与历史问题更新 AGENTS.md

请全面检查当前项目，并结合当前会话历史中已经讨论、开发、测试和修复过的内容，更新项目中的 `AGENTS.md` 文档。

本次任务的核心目标不是总结项目技术细节，而是将已经验证有效的开发规范、测试要求、常见错误、防错规则和文档维护机制沉淀下来，为后续 Codex 会话和其他开发 Agent 提供明确、可执行的约束，避免重复出现相同问题。

---

## 一、任务目标

请完成以下工作：

1. 分析当前项目的真实状态，包括：

   * 当前代码目录和模块划分；
   * 已实现功能；
   * 当前测试体系；
   * 配置文件、脚本和文档；
   * 最近新增、修改或重构的内容；
   * 当前尚未解决或需要特别注意的问题。

2. 回顾当前会话历史中与本项目有关的开发过程，重点提取：

   * 测试中曾经出现的错误；
   * 功能实现中出现过的问题；
   * 曾经遗漏的步骤；
   * 不正确的技术假设；
   * 接口、数据结构、配置、依赖或路径方面的问题；
   * 前端、后端、测试、部署、文档之间不同步的问题；
   * 已经明确要求修复、但容易在后续开发中再次出现的问题；
   * 用户曾经纠正过的实现方向或规则；
   * 已经验证有效的解决办法。

3. 将上述经验转化为明确的规则，更新到 `AGENTS.md` 中。

4. 建立持续维护机制：后续任何新功能开发、功能修改、重构、接口变化、测试策略变化或配置变化，都必须自动检查并同步更新 `AGENTS.md` 及相关文档。

---

## 二、分析依据

更新前必须综合检查以下信息，不得只根据单个文件或当前代码片段进行判断：

* 当前会话历史；
* 根目录及子目录中的现有 `AGENTS.md`；
* `README`、设计方案、开发计划和其他项目文档；
* Git 状态、最近修改记录和代码差异；
* 当前源码；
* 测试代码；
* 测试执行结果和错误日志；
* 构建、启动、检查、格式化和部署脚本；
* 依赖与配置文件；
* CI/CD 配置；
* 示例数据、测试夹具和环境变量示例。

如果仓库中存在多个 `AGENTS.md`，需要先明确它们各自的作用域和继承关系：

* 根目录 `AGENTS.md`：只记录适用于整个项目的通用规则；
* 子目录 `AGENTS.md`：只记录该目录或模块特有的规则；
* 不要在多个文件中机械复制相同内容；
* 子目录规则不得无意中与根目录规则冲突；
* 如确实需要覆盖上级规则，必须明确写出覆盖原因和适用范围。

---

## 三、错误与问题整理要求

请从历史开发和测试过程提取问题，但不要简单复制错误日志，也不要写成长篇复盘报告。

每个值得沉淀的问题，应转化为一条或一组可执行规则。规则至少应明确以下内容：

* 什么情况下容易发生；
* 开发 Agent 必须执行什么操作；
* 禁止采用什么做法；
* 修改后必须运行哪些检查或测试；
* 如何判断该问题已经得到正确处理。

推荐采用以下形式：

### 规则名称

* **触发条件**：在什么类型的任务中适用。
* **必须执行**：Agent 必须完成的动作。
* **禁止事项**：不得采用的实现方式或不得跳过的步骤。
* **验证方式**：需要运行的测试、检查或人工核对内容。
* **相关文件**：仅列出真正需要检查的关键文件或目录。

不要将问题写成：

> 某次开发中出现了某某错误，后来进行了某某修复。

应将其转化为：

> 修改某类模块时，必须同步检查某些接口、调用方和测试；不得只修改单一实现文件。完成后必须执行指定测试并确认不存在旧字段或旧路径引用。

---

## 四、AGENTS.md 的定位

`AGENTS.md` 是给后续开发 Agent 使用的项目执行规则文档，不是以下类型的文档：

* 项目技术报告；
* 完整架构设计说明；
* 产品需求文档；
* 项目发展历史；
* 所有功能的详细介绍；
* 所有源码文件的逐项说明；
* 大量背景知识或实现原理；
* 冗长的测试结果汇总；
* 当前代码状态的流水账。

因此，不要为了“完整”而把整个项目的技术细节全部写进 `AGENTS.md`。

只保留会直接影响后续开发正确性的内容，包括：

* 项目级开发约束；
* 模块边界和修改边界；
* 必须保持的不变量；
* 已知高风险区域；
* 已发生问题对应的防错规则；
* 测试和验证要求；
* 文档同步规则；
* 数据兼容性要求；
* 接口兼容性要求；
* 配置与环境要求；
* 禁止事项；
* 任务完成标准。

对于详细架构、模块原理、接口定义和使用说明，应放在对应的设计文档、README、API 文档或模块文档中，`AGENTS.md` 只引用其路径，不重复大段内容。

---

## 五、建议的 AGENTS.md 结构

请根据项目实际情况调整，但整体应保持紧凑、规则化和易检索。

```markdown
# AGENTS.md

## 1. 文档用途与适用范围

说明该文件约束哪些目录、哪些开发任务，以及与子目录 AGENTS.md 的关系。

## 2. 开发前必须完成的检查

例如：

- 阅读哪些规则文件；
- 查看哪些设计或任务文档；
- 检查当前 Git 状态；
- 确认修改范围；
- 确认现有测试和接口；
- 不得在未理解现有实现时直接重写。

## 3. 项目级核心约束

只记录跨模块长期有效的重要规则，例如：

- 架构边界；
- 数据流约束；
- 接口兼容规则；
- 配置管理规则；
- 依赖管理规则；
- 文件和目录组织规则。

## 4. 功能开发与修改规则

覆盖：

- 新功能开发；
- 已有功能修改；
- 重构；
- Bug 修复；
- 删除或废弃功能；
- 数据结构和接口变化。

## 5. 历史问题与防复发规则

将历史测试和开发中出现的问题转化为明确的执行规则。

按主题分类，例如：

- 接口与数据结构；
- 前后端同步；
- 配置与环境；
- 状态管理；
- 异步流程；
- 文件路径；
- 兼容性；
- 测试遗漏；
- 错误处理；
- 文档不同步。

不要按时间顺序写项目日志。

## 6. 测试与验证要求

明确不同修改类型对应的最小验证集合，例如：

- 单元测试；
- 集成测试；
- 端到端测试；
- 类型检查；
- 静态检查；
- 格式化检查；
- 构建验证；
- 启动验证；
- 关键人工核对。

## 7. 文档同步规则

明确哪些变更必须更新哪些文档。

## 8. 完成任务前的检查清单

形成简短、可逐项执行的 Definition of Done。
```

---

## 六、必须新增的文档自动维护规则

请在 `AGENTS.md` 中加入一套明确的“文档持续同步机制”。

至少包括以下规则：

### 1. 新功能开发

每当新增功能时，Agent 必须在任务完成前检查：

* 是否引入新的模块、目录、命令、配置、环境变量或依赖；
* 是否改变用户使用方式；
* 是否增加新的接口、数据结构或状态；
* 是否产生新的测试要求；
* 是否需要增加新的开发约束。

根据检查结果，同步更新：

* `AGENTS.md`；
* `README`；
* 对应模块文档；
* API 或数据结构文档；
* 配置说明；
* 测试说明；
* 设计文档或变更记录。

### 2. 功能修改或重构

每当修改已有功能、调整接口、重构代码或改变目录结构时，Agent 必须检查：

* 现有文档描述是否仍然准确；
* `AGENTS.md` 中的规则是否仍然适用；
* 旧命令、旧路径、旧字段和旧接口是否仍残留在文档中；
* 测试规则和验证命令是否需要调整；
* 是否产生新的风险或防错规则。

不得只修改代码而忽略文档同步。

### 3. Bug 修复和测试问题

每当修复 Bug 或解决测试失败时，Agent 必须判断该问题是否具有重复发生的可能性。

如果问题可能再次出现，则必须：

* 将问题的根因转化为一条可执行规则；
* 写入适当层级的 `AGENTS.md`；
* 增加或完善对应自动化测试；
* 更新相关文档；
* 避免只记录错误现象而不记录防错措施。

如果判断无需更新 `AGENTS.md`，应在任务总结中明确说明原因。

### 4. 文档更新是任务完成条件

任何新功能、功能修改、重构、Bug 修复、接口变化、数据格式变化或配置变化，在完成前都必须执行一次文档影响检查。

任务最终报告中必须明确列出：

* 已更新的文档；
* 检查后确认无需更新的文档；
* `AGENTS.md` 是否更新；
* 若未更新，具体原因是什么。

不得以“代码已经通过测试”为理由跳过文档检查。

### 5. 保持文档精简

更新 `AGENTS.md` 时必须：

* 优先修改现有规则，而不是不断追加近似规则；
* 合并重复内容；
* 删除已经失效的规则；
* 避免记录只对单次任务有效的临时信息；
* 将详细技术内容放入专项文档；
* 在 `AGENTS.md` 中仅保留规则摘要和文档链接；
* 保证每条规则都能指导具体开发行为或验证动作。

---

## 七、规则编写标准

写入 `AGENTS.md` 的规则必须满足：

1. **可执行**

   * 使用“必须”“不得”“修改后需要运行”等明确措辞；
   * 避免“建议关注”“尽量保证”“最好检查”等模糊表达。

2. **可验证**

   * 尽可能给出对应命令、测试范围或检查方法；
   * 不写无法判断是否完成的抽象要求。

3. **有适用范围**

   * 明确规则适用于整个项目、特定模块或特定变更类型；
   * 避免让局部规则错误地约束整个仓库。

4. **不过度绑定当前实现**

   * 只在必要时引用具体路径、模块或命令；
   * 不要把可能频繁变化的实现细节写成永久规则；
   * 对可能变化的内容优先引用专项文档。

5. **避免重复**

   * 相同含义只保留一处；
   * 新发现的问题优先补充已有规则，而不是新增近似章节。

6. **基于事实**

   * 只记录能够从代码、测试、日志、文档或会话历史中确认的内容；
   * 不得虚构不存在的问题、架构或测试命令；
   * 对无法确认的内容标记为待核实事项，不要直接写成强制规则。

---

## 八、执行流程

请按照以下顺序完成任务。

### 阶段 1：检查现状

* 定位所有 `AGENTS.md`；
* 阅读项目核心文档；
* 分析源码目录；
* 检查测试、脚本、配置和依赖；
* 检查当前 Git 变更；
* 回顾当前会话历史；
* 提取已经发生的错误、遗漏和用户纠正事项。

### 阶段 2：形成问题清单

在内部整理问题，至少区分：

* 已确认的历史问题；
* 已确认的高风险开发区域；
* 已有但不充分的规则；
* 已失效或错误的规则；
* 重复、冗长或技术报告化的内容；
* 当前缺失的测试要求；
* 当前缺失的文档同步机制。

此问题清单主要用于指导修改，不要将其原样全部堆入 `AGENTS.md`。

### 阶段 3：更新 AGENTS.md

* 保留仍然有效的现有规则；
* 修正不准确内容；
* 删除冗余和失效内容；
* 合并重复规则；
* 补充历史问题对应的防复发规则；
* 补充测试与验证要求；
* 补充文档自动维护机制；
* 保持结构简洁、清晰、方便 Agent 快速执行。

### 阶段 4：同步相关文档

如果更新 `AGENTS.md` 时发现其他文档存在过期、冲突或缺失，应同步修改。

但不要借本次任务无边界重写全部项目文档。只修改与当前规则、测试、命令、路径、接口或维护机制直接相关的内容。

### 阶段 5：验证

完成修改后检查：

* Markdown 结构是否正确；
* 文档中的路径是否真实存在；
* 命令是否可以执行；
* 测试名称是否准确；
* 是否存在互相冲突的规则；
* 是否存在重复条款；
* 是否仍然包含大量技术报告式内容；
* 是否已明确加入“功能变化必须同步更新文档”的规则；
* 是否已把历史错误转化为具体防错要求。

如果只修改文档且不涉及运行时代码，也至少要执行仓库现有的文档格式检查、链接检查或 Markdown 检查；如果项目没有这些检查，应进行人工核对并说明。

---

## 九、修改边界

本次任务主要修改文档，不得为了让文档看起来一致而随意修改业务代码。

只有在以下情况下才能修改代码或测试：

* 发现文档引用的命令因明显错误而无法运行；
* 发现已确认的历史问题缺少必要的回归测试，并且增加测试风险较低；
* 发现代码与已经确定的项目规则存在明确冲突；
* 当前会话中已经明确要求同步修复对应问题。

如需修改代码，必须保持变更最小，并在最终总结中单独说明。

---

## 十、最终输出要求

完成后请给出一份简洁的执行总结，包含：

1. 更新了哪些 `AGENTS.md`，各自适用范围是什么；
2. 从历史开发和测试中提炼了哪些主要防错规则；
3. 删除或合并了哪些重复、失效或技术报告化内容；
4. 新增了哪些文档持续同步规则；
5. 同步更新了哪些其他文档；
6. 执行了哪些检查或测试，结果如何；
7. 是否仍有无法从当前项目或会话历史中确认的事项；
8. 本次实际修改的文件列表。

不要只给出修改建议，必须直接检查并修改项目中的相应文件。

---

## 十一、验收标准

只有同时满足以下条件，任务才算完成：

* `AGENTS.md` 主要内容是规则和约束，而不是项目技术报告；
* 历史错误已经被转化为可执行的防复发规则；
* 规则包含明确的触发条件、执行动作和验证方式；
* 已加入新功能、功能修改、重构和 Bug 修复后的文档同步机制；
* 已明确要求每次任务结束前检查是否需要更新 `AGENTS.md`；
* 已明确要求同步维护其他相关文档；
* 没有虚构项目情况、测试命令或历史问题；
* 没有大范围重复已有技术文档；
* 没有机械追加大量重复规则；
* 文档结构简洁，后续 Agent 可以快速理解并执行；
* 所有修改均与当前项目实际状态一致。
