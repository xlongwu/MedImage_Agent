[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -ra
    --strict-markers
    --tb=short
markers =
    slow: tests that are expected to take longer than ordinary unit tests.
    gpu: tests that require a CUDA GPU and CuPy-compatible environment.
