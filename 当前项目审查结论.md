一、总体结论

当前项目可以被认定为一个合格的“受控型、领域专用 Agent 工程原型”，但还不能被认定为一个完整的、产品级的自主 Agent 系统。

更准确的项目定位是：

面向 rs-fMRI 科研处理的、Human-in-the-loop、Plan-then-Execute 型 Agentic Workflow Platform。

它已经不只是普通 WebUI，也不只是把大模型接到几个接口上的聊天应用。项目具备规划、上下文注入、工具目录、计划验证、人工审批、确定性执行、状态记录、审计、错误诊断和安全重试等 Agent 核心组成部分。项目还明确将 LLM 限制为“规划和建议层”，将真正的数据处理交给确定性的 Pipeline Runtime，这种设计对于医学科研场景是合理的。

但它目前更接近一个高度工程化的医学影像流水线编排器，Agent 的自主规划、环境观察、结果反思、动态重规划、长期记忆和自动恢复能力仍然较弱。

综合判断：

评价对象	当前水平
医学影像科研处理平台	约 8/10
受控型领域 Agent	约 7/10
完整产品级 Agent	约 5/10
开放式自主 Agent	不适合作为当前目标

这里的“不完整”并不代表应该让模型自由操作医学数据。相反，医学科研 Agent 应继续保持受控，只需要补齐受约束的自主闭环，而不是发展成无边界自动执行系统。

二、当前项目实际实现了什么
1. 已经形成了比较完整的 Agent 工程骨架

当前项目的主架构可以概括为：

用户科研目标
    ↓
项目上下文加载
    ↓
Planner 生成候选计划
    ↓
Plan Validator 静态验证
    ↓
计划审查与持久化
    ↓
Approval Gate 人工审批
    ↓
Plan Adapter + Safe Allowlist
    ↓
审计与执行一致性检查
    ↓
Pipeline Runtime
    ↓
节点执行、状态记录、产物注册
    ↓
运行检查、错误诊断、受控重试

这一结构符合领域 Agent 的基本组成，而不是单纯的 LLM 问答系统。

已实现的规划能力

项目存在两套主要规划入口：

根据自然语言目标匹配已有流水线节点；
通过 OpenAI-compatible API 让模型生成结构化 Pipeline Plan；
将当前项目的 BIDS、NIfTI、转换状态等上下文注入规划过程；
对模型生成的节点和依赖关系进行确定性验证。

llm_planner.py 当前默认仍是关键词和规则驱动，能够根据 realignment、ALFF、ReHo、smoothing、full preprocessing 等目标选择节点序列；同时保留 openai_compatible 模型入口。

这说明项目已经有 Planner，但它目前主要是：

模板选择器；
关键词路由器；
受 Tool Catalog 限制的 JSON 计划生成器。

它还不是能根据复杂科研目标自主拆解任务、比较多个方案并动态修改计划的高级 Planner。

已实现的计划验证

Plan Validator 已经实现了较完整的静态验证，包括：

Pipeline 结构校验；
节点 ID 是否存在于 Tool Catalog；
重复节点检查；
依赖节点存在性检查；
自依赖和环路检测；
高风险节点识别；
人工操作节点识别；
后端类型不一致提醒；
部分节点参数验证。

这部分是目前项目比较成熟的 Agent 组件。

已实现的计划持久化和一致性绑定

较新的 Reviewed Plan 路径已经能够：

对计划计算稳定哈希；
生成稳定的 reviewed_plan_id；
将计划快照写入项目目录；
将计划和具体项目、配置路径绑定；
在执行前检查提交计划是否与已审查计划一致；
拒绝不同项目、不同配置或不同哈希的计划。

这是产品化 Agent 非常重要的能力。

另外，执行一致性模块还会比较：

Plan Hash；
Reviewed Plan ID；
Project ID；
节点集合；
节点参数哈希；
输出根目录；
输出 Manifest；
Safe Allowlist 指纹；
Approval 和 Audit 上下文。

这已经接近一个规范的 Agent Execution Contract。

2. 已经实现了较强的人工审批和安全执行机制

当前 Approval Gate 不只是一个简单的 approved=true。

在较新的 Reviewed Execution 路径中，它支持：

节点级审批；
后端级审批；
禁止使用通配符批准 MATLAB、SPM、DPABI 等高风险节点；
Rawdata 只读确认；
输出目录确认；
风险知情确认；
覆盖策略确认；
受试者范围确认；
Native preprocessing 专用确认；
Manual/GUI 节点阻止自动执行。

/api/plans/execute-reviewed 目前是项目中最成熟、最应该保留的执行入口。它在调用 Pipeline Runtime 之前依次执行：

环境变量执行开关；
用户明确确认；
强制持久化审计；
Project Config 校验；
Project Context 校验；
Reviewed Plan 匹配；
重新验证计划；
Approval Gate；
Plan Adapter；
Safe Allowlist；
Native preprocessing readiness；
Pipeline YAML 生成；
审计记录；
Dry-run 与正式执行一致性校验；
Pipeline Runtime 执行。

这一部分已经达到“合格的高风险领域 Agent 执行网关”水平。

3. 已经实现了确定性 Pipeline Runtime

Pipeline Runtime 当前具备：

Pipeline YAML 加载；
节点依赖检查；
注册节点 Runner 调用；
Project-level 与 Subject-level 节点；
多受试者并行调度；
CPU、MATLAB 和 GPU Worker 配置；
节点级状态记录；
失败受试者隔离；
stop_on_failure；
Pipeline Summary；
运行时长和错误汇总。

它不是让大模型直接执行 Python、MATLAB 或文件命令，而是让模型只能选择经过注册的节点，再由确定性 Runner 执行。这种边界设计是正确的。

但它目前仍是“按 Pipeline 中节点列表顺序执行并检查依赖”，还不是一个真正会动态选择所有 Ready Nodes 的通用 DAG 调度器。

4. 医学影像处理能力已经不只是占位实现

根据当前能力矩阵，项目已经实现了以下 Native Python 数值处理能力：

Slice Timing；
简化 Realignment；
简化 Coregistration；
简化 Segmentation 和 Affine Normalization；
Smoothing；
Nuisance Regression；
Temporal Filtering；
ALFF；
fALFF；
ReHo；
ROI 时间序列提取；
Pearson FC；
Fisher-z；
Atlas 加载、形状和仿射检查；
DICOM 转换后的 BIDS/NIfTI 数据交接。

其中：

ReHo CPU 路径具有 Golden Validation；
Pearson FC Kernel 具有参考验证；
GPU ReHo 因 ties 处理问题仍未验证，并会回退 CPU；
简化 Realignment、Coregistration 和 Normalization 不能等价宣称为 SPM 标准实现；
完整 DICOM→Reviewed FC 的打包 GUI E2E 尚未验证。

项目能够明确区分：

Contract Only；
Dry-run；
Sandbox Scaffold；
Metadata Only；
Numerically Implemented；
Reference Validated；
Release Ready。

这种能力真实性分级非常好，可以避免把预览结果或元数据占位文件错误展示为真实科研计算结果。

5. 已经实现了状态、审计和部分记忆能力

项目有 SQLite SessionDB，用于保存和查询：

Run；
Node；
Error；
Subject；
执行状态；
输出、警告和错误；
FTS5 全文索引。

这属于运行记忆和审计记忆，但还不是 Agent 的真正长期记忆。目前它不会自动从历史成功方案中学习，也不会主动检索“类似数据集过去使用了哪些参数”。

6. 已经实现错误诊断和受控重试

项目能够：

读取运行状态和日志；
对错误进行规则分类；
给出可能原因；
给出修复建议；
判断是否适合安全重试；
生成 Retry Plan；
对允许的节点执行人工批准后的重试。

但诊断报告明确说明：

Retry Plan 只是建议，不会自动执行。

因此当前是“有恢复工具”，而不是“有自主恢复闭环”。

7. GUI Agent 目前仍是安全原型

虽然代码中存在 PyWinAuto Provider，实现了窗口查找、点击、文本输入、菜单选择和截图接口，但当前 Guard 明确只允许 provider="mock"，真实桌面 Provider 被无条件阻止。

当前 GUI Agent 实际只能：

创建 Session；
记录模型拟执行的动作；
运行 Action Guard；
写审计记录；
生成 Replay Script；
产生模拟截图占位文件。

它不会真正控制 SPM 或 DPABI 桌面。因此这部分应标注为：

GUI Agent Guard / Mock Adapter Prototype

而不是已经完成的 GUI Agent。

三、当前项目最严重的问题
P0：旧版 Agent 执行入口破坏了新版安全模型

这是本次检查发现的最重要问题。

旧接口 /api/agent/execute 只接收：

agent_run_id；
project_config_path；
pipeline_path；
一个布尔值 approved。

该接口固定从：

outputs/work/agent_runs/{agent_run_id}/plan.json

读取计划，但计划生成器实际上会根据 Project Config 中的 work_dir 写入计划。这在自定义 work_dir 下可能出现路径不一致。

更加严重的是，Agent Runtime 发现：

plan.pipeline_path != 请求中的 pipeline_path

时，仅追加一个 Warning，随后仍然执行请求提供的 pipeline_path。

这意味着旧路径没有强制保证：

被执行的 Pipeline，就是用户之前审查和批准的 Pipeline。

虽然新版 execute-reviewed 已经通过 Plan Hash、Reviewed Plan、Audit 和 Execution Consistency 解决了这一问题，但旧接口仍然存在。

必须修改
禁止旧 /api/agent/execute 直接调用 Runtime；
将其改为兼容层，内部强制转到 /api/plans/execute-reviewed；
或直接废弃旧入口；
所有执行必须使用 reviewed_plan_id + plan_hash + approval_context_id；
Pipeline 路径不一致必须直接阻止执行，不能只产生警告。
P0：ToolSpec 的路径权限目前主要是描述信息

ToolSpec 定义了：

allowed_read_paths；
allowed_write_paths；
writes_files；
destructive；
requires_confirmation。

但当前 assert_tool_allowed() 实际只检查工具是否存在，以及是否传入 approved=true，并没有验证本次执行实际访问的输入和输出路径是否落在允许范围内。

因此 Tool Registry 目前更接近权限元数据目录，还不是完整的 Capability Enforcement Layer。

需要在 Runner 调用前加入统一执行上下文：

ToolExecutionContext
- project_id
- actor_id
- approved_plan_hash
- allowed_input_roots
- allowed_output_roots
- permitted_node_ids
- execution_ticket
- audit_id

每个文件访问和外部进程调用都应从该上下文获取权限，而不是依靠节点自行遵守约定。

P0：所有执行入口尚未收敛到同一个安全网关

当前至少存在：

Legacy Agent Execute；
Planner Execute；
Execute Reviewed；
Retry Execute；
Native preprocessing execute；
DICOM conversion execute；
DPABI、SPM、GPU 相关执行入口。

其中 execute-reviewed 的设计最完整，但其他入口并不一定全部经过相同的：

Plan Hash；
Approval Gate；
Actor Identity；
Audit；
Execution Consistency；
Safe Allowlist；
Path Capability。

完整 Agent 项目不应该存在多套不同安全等级的执行链。

四、距离完整 Agent 还缺少哪些能力
1. 缺少统一的 Agent 状态机

现在的 Planner、Validator、Approval、Executor、Diagnosis 和 Retry 都存在，但仍然是多个独立接口。

建议建立统一状态机：

CREATED
→ CONTEXT_READY
→ PLAN_DRAFTED
→ PLAN_VALIDATED
→ WAITING_FOR_APPROVAL
→ APPROVED
→ EXECUTION_READY
→ RUNNING
→ OBSERVING
→ SUCCEEDED

失败分支：
RUNNING
→ FAILED
→ DIAGNOSING
→ RETRY_PROPOSED
→ WAITING_FOR_RETRY_APPROVAL
→ RETRYING
→ SUCCEEDED / HUMAN_HANDOFF

由 Orchestrator 保存和推进状态，而不是由前端决定下一步应该调用哪个接口。

2. 缺少真正的执行后观察与动态重规划

目前 Pipeline 执行结束后，系统可以检查状态、诊断错误并生成 Retry Plan，但不会自动：

读取执行结果；
判断目标是否完成；
判断输出是否满足科研要求；
调整参数；
移除不可执行节点；
选择替代工具；
重新生成部分计划；
在审批范围内执行安全重试。

完整的受控闭环应为：

Plan
→ Execute
→ Observe
→ Evaluate
→ Decide
   ├─ Finish
   ├─ Safe Retry
   ├─ Replan
   └─ Human Handoff

这里可以限制：

最大重规划次数；
最大重试次数；
只允许低风险节点自动重试；
节点集合或输出范围变化时重新审批；
高风险后端始终人工审批。
3. Planner 的智能程度仍然不足

当前规则 Planner 主要按关键词选择模板；模型 Planner 主要要求模型输出符合格式的 JSON。它尚未系统支持：

任务分解；
多候选计划生成；
计划比较与排序；
依据数据状态选择参数；
前置条件推理；
工具输入输出匹配；
缺失数据识别；
预算和资源约束；
失败后的局部重规划；
计划置信度；
对不确定内容主动请求人工确认。

模型输出目前也是普通文本 JSON，再通过 json.loads 解析，尚未使用严格 JSON Schema 或 Provider 的 Structured Output 能力。

4. 大多数节点缺少严格参数 Schema

Plan Validator 对节点结构、依赖和节点 ID 检查得很好，但参数级验证目前主要只针对少数节点，例如 SPM Realign。

每一个可执行节点都应该定义：

input_schema
parameter_schema
output_schema
preconditions
postconditions
side_effects
resource_requirements
retry_policy
idempotency_policy
validation_policy

否则模型虽然不能虚构节点，但仍可能给真实节点生成错误或不合理参数。

5. 缺少真正的 Agent 长期记忆

当前 SessionDB 能保存运行历史，但尚未形成：

项目级科研偏好；
常用预处理方案；
历史成功参数；
不同数据状态对应的最佳方案；
常见失败及其解决记录；
受试者排除决策；
Atlas 和模板偏好；
用户明确批准过的安全策略。

应增加结构化 Memory，而不是简单把所有历史文本交给模型。

比较合适的记忆层次是：

Working Memory：当前运行上下文；
Episodic Memory：历史运行及其结果；
Semantic Memory：科研流程知识和错误知识库；
Project Memory：当前项目稳定事实；
User Preference Memory：用户明确保存的偏好。

所有写入长期记忆的内容都需要来源、置信度和审核状态。

6. 缺少 Agent 专项评测体系

当前项目有科学计算 Golden Test、Backend Test、Frontend Test 和部分 E2E Test，这是良好基础。仓库状态文档记录过 109 passed, 4 skipped，但这是已有仓库验证记录，不是本次检查在独立环境中的复现结果。

完整 Agent 还需要专门评测：

给定目标，是否选对 Pipeline；
是否会漏掉必要节点；
是否会添加冗余节点；
是否能发现缺失输入；
是否正确识别高风险行为；
是否拒绝未知节点；
是否错误批准未经审查的计划；
面对运行失败是否能正确诊断；
是否能进行最小范围重试；
是否在计划变化后重新申请审批；
Prompt Injection 是否能绕过 Tool Catalog；
不同模型和 Prompt 版本的规划稳定性；
真实多受试者 DICOM→BIDS→预处理→FC 的完整 E2E。
7. 缺少产品级身份认证和多用户授权

FastAPI 入口目前可以看到 CORS、Request ID、日志、Rate Limit 和 API Version Middleware，但没有看到统一的身份认证或 RBAC 中间件。

如果项目严格限定为单机 Electron 桌面应用，这一问题暂时可以接受。

一旦作为局域网 WebUI、服务器平台或多人科研平台，就必须加入：

用户认证；
项目级权限；
审批人身份；
操作者与审批者分离；
Approval Record 防篡改；
API Token 或本地 Session Token；
审计日志签名；
PHI/敏感路径访问控制。

不能继续把请求中的 approved_by="local-user" 当成可信身份。

8. 通用 Pipeline Runtime 还需要进一步产品化

当前 Generic Pipeline Runtime 使用进程内同步执行和 ThreadPoolExecutor。这适合本地原型，但完整产品还需要：

持久化任务队列；
Worker 进程隔离；
心跳和租约；
崩溃恢复；
幂等提交；
节点 Checkpoint；
Pause、Resume、Cancel；
超时；
资源配额；
GPU 显存调度；
MATLAB License 并发限制；
Executor 重启后恢复；
多项目任务隔离。

Native preprocessing 已经开始出现进度和取消状态，但这些能力仍需统一到通用 Agent Runtime。

9. GUI Agent 需要真实实现或明确移出主线

当前 GUI Agent 只允许 mock。继续开发前需要先决定：

方案 A：不做 GUI Agent

优先通过 MATLAB Engine、SPM Batch、DPABI Function Wrapper 等确定性接口完成调用。这对科研可重复性更好。

方案 B：保留 GUI Agent

则需要补齐：

真实视觉观察；
窗口和控件稳定定位；
截图脱敏；
单步确认；
异常窗口停止；
坐标漂移处理；
应用版本识别；
可回放记录；
沙箱桌面；
真实 GUI E2E；
窗口外操作阻止。

对于医学科研软件，优先建议方案 A，GUI Agent 只作为极少数无 API 场景的补充。

五、建议的目标架构

建议最终统一为以下结构：

User Goal
    ↓
Context Builder
    ├─ Project Context
    ├─ Dataset State
    ├─ Tool Capability
    ├─ Historical Runs
    └─ User Constraints
    ↓
Structured Planner
    ├─ Candidate Plans
    ├─ Preconditions
    ├─ Expected Outputs
    ├─ Risks
    └─ Confidence
    ↓
Deterministic Validator
    ↓
Immutable Reviewed Plan
    ↓
Signed Approval Context
    ↓
Execution Ticket
    ↓
Durable Agent Orchestrator
    ↓
Capability-enforced Tool Runtime
    ↓
Observation / Scientific Validation
    ↓
Bounded Retry or Replan
    ↓
Final Report + Memory Update Proposal

其中最关键的新对象是 Execution Ticket：

execution_ticket_id
project_id
reviewed_plan_id
plan_hash
approval_context_id
approved_actor
approved_node_ids
approved_backend_ids
allowed_input_roots
allowed_output_roots
safe_allowlist_fingerprint
expires_at
max_retry_count
audit_id

Pipeline Runtime 只接受有效 Execution Ticket，不再接受任意 pipeline_path + approved=true。

六、推荐的建设优先级
第一阶段：安全执行入口收敛

必须先完成：

废弃或重构旧 /api/agent/execute；
所有执行统一进入 Reviewed Execution Gateway；
Pipeline、Approval、Project Context、Allowlist 全部使用哈希绑定；
Tool Path 权限从元数据升级为运行时强制检查；
为每个节点定义参数和输入输出 Schema；
增加真实用户身份和 Approval Context；
清理重复的 Planner 和 LLM Provider 实现。
第二阶段：补齐 Agent 闭环

重点完成：

统一 Agent 状态机；
自动收集执行观察；
增加目标完成度判断；
增加受限 Safe Retry；
增加局部 Replan；
计划变化自动触发重新审批；
增加 Pause、Resume、Cancel、Checkpoint。
第三阶段：增强智能和记忆

重点完成：

Structured Output Planner；
多候选计划比较；
历史运行检索；
项目级长期记忆；
错误经验库；
参数推荐及证据来源；
Agent 专项评测集和回归测试。
第四阶段：科学验证和产品化

重点完成：

真实多受试者完整 E2E；
Native 实现与 SPM/DPABI 独立参考比较；
安装包 GUI Smoke；
可复现依赖锁定；
SBOM 和依赖安全扫描；
多用户权限；
运行监控和资源管理；
根据项目定位决定是否加入组统计、机器学习分析和报告编辑。
七、最终判断

当前项目的名称使用 “MedImage Agent” 是合理的，但对外描述应当保持为：

一个面向 rs-fMRI 科研流程的、确定性执行、人工审批、可审计的受控 Agent 平台。

目前不适合宣称：

完整自主医学影像 Agent；
能自主完成任意医学影像研究；
已完成真实 GUI Agent；
已完整验证 SPM/DPABI；
已具备自动纠错和自主重规划；
可用于临床诊断或临床决策。

项目当前最大的优势不是模型有多智能，而是安全边界、计划审查、执行真实性和科研能力分级做得比较认真。最大的不足则是新旧执行架构并存、Agent 闭环尚未打通、Planner 智能程度有限。

最优先的工作不是继续增加更多页面或更多预处理节点，而是：

先关闭旧执行路径，统一执行契约，再把现有 Planner、Validator、Approval、Executor、Diagnosis、Retry 串成一个正式的 Agent 状态机。