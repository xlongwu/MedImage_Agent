# 任务 10B：目标编排、科学决策与集中审批

> 状态：Blocked — requires Phase 9 `G9-6` and 10A `G10-1`
>
> 任务模式：Architecture and Refactor Mode + Feature Bundle Mode
>
> 交付目标：让 BIDS 项目从一句目标自动走到一次集中审批，并继续使用现有 Reviewed Plan、Approval Gate、Execution Ticket 和 Gateway。

## 1. Current Gap

- `/api/planner/plan-from-goal` 能加载 context 和 validate plan，但只返回响应，不创建完整 lifecycle：`src/backend/app/api/llm_planner_routes.py:46-98`。
- reviewed execution 已包含 validation、approval、audit、ticket、gateway 和 lifecycle，但逻辑集中在大型 route：`src/backend/app/api/execute_reviewed_routes.py:726-1184`。
- lifecycle 没有“等待补充信息”“等待科学决策”“取消”状态：`src/backend/app/schemas/agent_lifecycle.py:15-38`。
- current transition table 把 HUMAN_HANDOFF 设为终态，不能拿它伪装可恢复问答：`src/backend/app/services/agent_orchestrator.py:59-81`。
- 前端当前要求用户点击多项 confirmation：`src/frontend/src/features/workspaces/PreprocessingReviewedFlow.tsx:60-122,643-688`。

## 2. Required Behavior

### 2.1 Canonical lifecycle 最小扩展

新增 canonical states：

- `WAITING_FOR_INPUT`：缺数据、缺资源或上下文不确定；
- `WAITING_FOR_SCIENCE_DECISION`：科学含义会变化的参数需要用户决定；
- `CANCELED`：只允许 dispatch 前取消。

新增 immutable pending decision payload，包含 decision_id、kind、question、options、recommended_option、impact、plan_hash_before。它属于 lifecycle 上下文，不保存独立状态机。

允许的关键转移：

```text
CREATED/CONTEXT_READY -> WAITING_FOR_INPUT -> CONTEXT_READY
PLAN_DRAFTED -> WAITING_FOR_SCIENCE_DECISION -> PLAN_DRAFTED
pre-dispatch states -> CANCELED
```

运行中不支持 cancel；不得把“关闭窗口”当取消。

### 2.2 Create command

`POST /agent/tasks` 使用有限编排：

1. 创建 lifecycle 并持久化用户 goal、goal hash、actor、command id；
2. 加载项目上下文和只读 readiness；
3. 缺输入则进入 WAITING_FOR_INPUT；
4. 调用抽取后的 goal planning service；
5. 检测 GSR、Atlas、TR、Template 等科学歧义；
6. 有歧义则进入 WAITING_FOR_SCIENCE_DECISION；
7. 生成 plan，运行 Plan Validator 和 native dry-run；
8. 构建 Goal Contract，并保存 approval_status=pending 的 Reviewed Plan；
9. 生成包含节点、后端、写入 roots、安全边界、局限和 confirmation expansion 的 Approval Summary；
10. 进入 WAITING_FOR_APPROVAL 并停止。

### 2.3 Answer command

- 只接受当前 pending decision_id；
- answer 持久化为 immutable command/evidence；
- 科学答案必须重建 plan 和所有 hashes；
- 如果 answer 导致新歧义，继续停在 decision；
- 不允许模型代表用户选择 Atlas、GSR 或其他改变科学含义的选项。

### 2.4 Approve command

前端只发送：

```json
{
  "command_id": "...",
  "actor": "...",
  "approval_summary_hash": "..."
}
```

服务端验证 current state、reviewed plan、plan hash、goal contract、write roots、node/backend allowlist、summary expiry 后，把批准映射到现有细粒度 confirmation，并调用共享 reviewed execution application service。任何 hash、路径、node、backend 或参数变化都返回 stale approval，重新进入 PLAN_DRAFTED/WAITING_FOR_APPROVAL。

## 3. Files

### Create

- `src/backend/app/services/goal_planning_service.py`
- `src/backend/app/services/reviewed_execution_service.py`
- `src/backend/app/services/agent_task_command_service.py`
- `src/backend/app/schemas/approval_summary.py`
- `tests/unit/test_agent_task_commands.py`
- `tests/unit/test_approval_summary.py`
- `tests/unit/test_reviewed_execution_service.py`

### Modify

- `src/backend/app/schemas/agent_lifecycle.py`
- `src/backend/app/services/agent_orchestrator.py`
- `src/backend/app/services/mock_store.py`
- `src/backend/app/api/llm_planner_routes.py`：改为薄 route，调用共享 service；
- `src/backend/app/api/execute_reviewed_routes.py`：改为薄 route，调用共享 service；
- `src/backend/app/api/agent_task_routes.py`：增加 create/answer/approve/cancel；
- `src/backend/app/schemas/agent_task.py`
- `src/backend/app/api/dependencies.py`
- `src/frontend/src/lib/types/agentTask.ts`
- `src/frontend/src/lib/api/agentTasks.ts`

### Read Only

- scientific kernels 和 node runner；
- `src/backend/app/runtime/execution_gateway.py`
- `src/backend/app/services/execution_ticket_service.py`
- `src/backend/app/planner/approval_gate.py`
- `src/backend/app/planner/goal_contract_builder.py`

若共享 service 提取无法在不修改 gateway/ticket 语义下完成，停止并提交受保护模块变更说明。

## 4. Detailed Tasks

1. 为 lifecycle schema 升级版本并增加旧 v1/v2 fixture round-trip。
2. 把 plan-from-goal 的 context/application 逻辑移入 service，保持旧 endpoint 响应兼容。
3. 为 profile 隐藏建立确定性 goal → stage graph policy；内部可继续用 `fc_minimal`/`dparsfa_like`，但响应默认不暴露 profile 名。
4. 定义科学决策矩阵：Atlas、GSR、TR 冲突、Template、覆盖已有 run、实验性 GPU；其余资源选择不询问。
5. 抽取 execute-reviewed application service；旧 route 所有 characterization tests 先绿再接 Agent route。
6. Approval Summary 计算 stable hash，写入范围使用规范化 roots，细粒度 confirmations 进入 audit。
7. create/answer/approve/cancel 全部 project-scoped、idempotent、并发安全。
8. 自动 dry-run 失败转成 next action，不生成可审批摘要。
9. BIDS path 打通后再交给 10C；DICOM 自动 execute 明确由 10E gate 管理。

## 5. Acceptance Criteria

- [ ] BIDS 标准 goal 产生 WAITING_FOR_APPROVAL，不需手动 dry-run/validation/report。
- [ ] GSR/Atlas 等歧义产生 WAITING_FOR_SCIENCE_DECISION，未回答不生成执行审批。
- [ ] 一次 approve 形成所有现有 confirmation、audit、ticket 并仅调用一次 Gateway。
- [ ] stale/tampered summary hash 在任何写入或 runner 前失败。
- [ ] scope expansion 自动使旧审批失效。
- [ ] cancel 只在 dispatch 前成功；运行中返回明确不支持。
- [ ] 旧 planner 与 execute-reviewed API 兼容测试不回退。
- [ ] LLM provider 不可用时可解释失败/请求用户处理，不得伪造 deterministic plan。

## 6. Hazard/Test Matrix

| Hazard | Required test |
|---|---|
| H10-02 gateway bypass | old/new route 都 monkeypatch 同一 service/gateway，dispatch count=1 |
| H10-03 approval weakening | granular confirmation snapshot、hash tamper、expiry、path change |
| H10-04 science mutation | GSR/atlas/TR matrix，answer replay，model cannot auto-answer |
| H10-05 unbounded loop | max transition count per command，no retry/approve in create |
| H10-11 migration | v1/v2 SQLite payload load、round-trip、new default fields |
| H10-13 path scope | symlink/path traversal/cross-project output roots |

## 7. Validation Commands

```text
python -m pytest tests/unit/test_agent_task_commands.py tests/unit/test_approval_summary.py tests/unit/test_reviewed_execution_service.py --tb=short --basetemp=.pytest_tmp
python -m pytest tests/unit/test_agent_lifecycle.py tests/unit/test_llm_planner_api.py tests/unit/test_execute_reviewed_api.py tests/unit/test_execution_ticket.py --tb=short --basetemp=.pytest_tmp
python -m pytest tests/unit/test_path_safety.py tests/unit/test_gui_reviewed_execution_blocklist.py --tb=short --basetemp=.pytest_tmp
```

共享执行基础设施改变后必须运行完整 backend suite。每次 pytest 后执行规定清理。

## 8. Stop Conditions

- 需要让 LLM 直接调用 runner；
- 无法通过现有 Approval Gate/Ticket/Gateway 完成；
- 需要修改 scientific formula 或默认科学参数；
- 需要在 10E 前自动调用 DICOM 主执行 endpoint；
- schema migration 无法读取现有 desktop SQLite；
- Phase 9 未关闭。

## 9. Completion Report Additions

除标准 Completion Report 外，附 transition diff、old/new route compatibility、approval hash proof、gateway call graph、granular confirmation snapshot、schema migration fixture 结果和仍需用户决定的科学参数清单。
