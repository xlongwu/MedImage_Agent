# Report Package Validator Specification

Validates exported rs-fMRI report packages for integrity: checksums, ZIP consistency, required files, safety flags, and forbidden binary content. Read-only from exports, writes only validation artifacts. Does not repair packages.
