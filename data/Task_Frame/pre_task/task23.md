你是我的工程搭建助手。前二十二步已经完成：

Step 1：完成项目工程骨架，并打通 MATLAB / SPM / DPABI 环境检查闭环。
Step 2：完成单节点执行闭环。
Step 3：完成最小 Pipeline DAG 执行闭环。
Step 4：完成 synthetic BIDS-like 数据集扫描与索引闭环。
Step 5：完成 synthetic subject-level SPM smoothing + QC 闭环。
Step 6：完成数据集级评估与 Markdown/HTML 报告闭环。
Step 7：完成 deterministic Agent Runtime、Plan Mode、Execute Mode、Tool Registry、Hook Manager 和 approval 机制。
Step 8：完成最小长期记忆、后台复盘和错误知识库闭环。
Step 9：完成最小 FastAPI 后端服务闭环。
Step 10：完成最小 React 前端闭环。
Step 11：完成 Run Monitor + State / Log Viewer 闭环。
Step 12：完成 Error Diagnosis + Retry Plan 闭环。
Step 13：完成 Checkpoint / Cache / Approved Retry 闭环。
Step 14：完成本地 subject-level 并行调度与资源限制闭环。
Step 15：完成 GPU ALFF / fALFF 原型与 CPU fallback 闭环。
Step 16：完成 Validation / Benchmark / Regression Suite 闭环。
Step 17：完成 DPABI Capability Inspector + Wrapper Scaffold 闭环。
Step 18：完成 DPABI Dataset Adapter + Batch Config Preflight 闭环。
Step 19：完成 DPABI 参数审查 + Approved Run Plan 闭环。
Step 20：完成 DPABI Approved Sandbox Smoke Run + Execution Audit 闭环。
Step 21：完成 DPABI Function Signature Probe + Wrapper Contract Registry 闭环。
Step 22：完成 DPABI Single-Function Wrapper Sandbox + Contract Test 闭环。

现在开始第二十三步。

第二十三步目标：实现“DPABI Single-Function Subject Wrapper + SPM Baseline Comparison 闭环”。

当前系统已经可以在 synthetic sandbox 中测试 `y_Smooth` 或 `rest_Smooth`，但还没有把 DPABI 单函数 wrapper 接入 subject-level pipeline。

本步骤要实现一个受控 subject-level DPABI smoothing wrapper：

- 读取 synthetic BIDS-like dataset_index.json
- 对每个 COMPLETE subject 获取 BOLD 输入
- 将输入复制或转换到 work/dpabi/subject_wrapper_workspace/{subject_id}/input.nii
- 调用 allowlisted DPABI 单函数：
  - y_Smooth
  - rest_Smooth
- 输出到 derivatives/dpabi_single_function/{subject_id}/func/
- 写 subject-level result JSON
- 写 subject-level state
- 聚合 dataset-level DPABI wrapper comparison report
- 可选与 SPM smoothing 输出做工程对比
- 生成 dpabi_subject_wrapper_summary.json
- 生成 dpabi_subject_wrapper_report.md
- 前端 DPABI Panel 显示 subject wrapper summary 和 report

本步骤仍然不要运行完整 DPABI pipeline。
本步骤不要调用 DPARSF_run / DPARSFA_run。
本步骤不要调用 DPABI GUI。
本步骤不要处理真实医学影像数据。
本步骤只允许处理项目自动生成的 synthetic BIDS 数据。
本步骤不要修改 rawdata。
本步骤不要修改 DPABI 源码。
本步骤不要删除文件。

---

## 1. 创建 specs/dpabi_subject_wrapper_spec.md

创建文件：

```text
specs/dpabi_subject_wrapper_spec.md

内容：

# DPABI Subject-Level Single-Function Wrapper Specification

This document defines the MVP subject-level DPABI single-function wrapper.

## Goals

The subject wrapper safely applies selected DPABI utility functions to synthetic subject-level data.

The MVP focuses on:

- y_Smooth
- rest_Smooth
- synthetic BIDS-like input only
- subject-level execution
- derivative output
- engineering comparison with SPM smoothing if available
- audit-friendly result JSON
- dataset-level summary report

## Scope

Supported in this step:

- read synthetic dataset_index.json
- run allowlisted DPABI smoothing function per subject
- write derivatives under derivatives/dpabi_single_function
- write subject result JSON
- aggregate dataset-level summary
- optional comparison with SPM smoothing outputs
- API and frontend visibility

Unsupported in this step:

- full DPABI preprocessing
- DPARSF_run execution
- DPARSFA_run execution
- DPABI GUI automation
- real medical image preprocessing
- rawdata modification
- DPABI source modification
- deletion of files
- clinical interpretation

## Inputs

```text
work/dataset_index/dataset_index.json
work/dpabi/dpabi_wrapper_contracts.json
examples/project_config_dataset.yaml
Outputs
derivatives/dpabi_single_function/{subject_id}/func/{subject_id}_dpabi_smooth.nii
derivatives/dpabi_single_function/{subject_id}/func/dpabi_subject_wrapper_result.json
reports/dpabi/dpabi_subject_wrapper_summary.json
reports/dpabi/dpabi_subject_wrapper_report.md
Allowlisted Functions
y_Smooth
rest_Smooth
Safety Rules
Do not run DPARSF_run.
Do not run DPARSFA_run.
Do not call DPABI GUI.
Do not modify rawdata.
Do not modify DPABI source.
Do not delete files.
Only process synthetic BIDS-like data generated by this project.
Treat outputs as engineering integration artifacts only.

---

## 2. 创建 matlab/dpabi_subject_smooth_wrapper.m

创建文件：

```text
matlab/dpabi_subject_smooth_wrapper.m

功能要求：

接收参数：
dpabi_dir
function_name
input_nii
output_nii
fwhm_json
output_json
只允许 function_name：
y_Smooth
rest_Smooth
添加 DPABI 路径：
addpath(genpath(dpabi_dir))
调用 allowlisted smoothing 函数。
输出：
output_nii
output_json
记录：
function_name
input_nii
output_nii
fwhm
call_pattern
wrapper_call_success
errors
warnings
严禁调用：
DPABI
DPARSF
DPARSFA
DPARSF_run
DPARSFA_run

参考实现：

function dpabi_subject_smooth_wrapper(dpabi_dir, function_name, input_nii, output_nii, fwhm_json, output_json)
    result = struct();
    result.ok = true;
    result.node_id = 'dpabi_subject_smooth_wrapper';
    result.backend = 'matlab-dpabi';
    result.function_name = function_name;
    result.input_nii = input_nii;
    result.output_nii = output_nii;
    result.outputs = {};
    result.metrics = struct();
    result.errors = {};
    result.warnings = {};
    result.matlab_version = version;

    try
        allowlist = {'y_Smooth', 'rest_Smooth'};
        if ~any(strcmp(function_name, allowlist))
            error(['Function is not allowlisted: ', function_name]);
        end

        if ~exist(dpabi_dir, 'dir')
            error(['DPABI directory not found: ', dpabi_dir]);
        end

        if ~exist(input_nii, 'file')
            error(['Input NIfTI not found: ', input_nii]);
        end

        addpath(genpath(dpabi_dir));

        fn_path = which(function_name);
        result.metrics.function_found = ~isempty(fn_path);
        result.metrics.function_path = fn_path;

        if isempty(fn_path)
            error(['Function not found on MATLAB path: ', function_name]);
        end

        fwhm = jsondecode(fwhm_json);
        if numel(fwhm) ~= 3
            error('FWHM must contain exactly 3 values.');
        end
        fwhm = double(fwhm(:)');

        output_dir = fileparts(output_nii);
        if ~exist(output_dir, 'dir')
            mkdir(output_dir);
        end

        result.metrics.wrapper_call_attempted = true;
        result.metrics.wrapper_call_success = false;
        result.metrics.fwhm = fwhm;

        if strcmp(function_name, 'y_Smooth')
            try
                y_Smooth(input_nii, output_nii, fwhm);
                result.metrics.wrapper_call_success = exist(output_nii, 'file') == 2;
                result.metrics.call_pattern = 'y_Smooth(input_nii, output_nii, fwhm)';
            catch ME1
                result.warnings{end+1} = ['First y_Smooth call pattern failed: ', ME1.message];

                try
                    y_Smooth({input_nii}, {output_nii}, fwhm);
                    result.metrics.wrapper_call_success = exist(output_nii, 'file') == 2;
                    result.metrics.call_pattern = 'y_Smooth({input_nii}, {output_nii}, fwhm)';
                catch ME2
                    error(['y_Smooth subject wrapper failed. Manual signature review required. Last error: ', ME2.message]);
                end
            end

        elseif strcmp(function_name, 'rest_Smooth')
            try
                rest_Smooth(input_nii, output_nii, fwhm);
                result.metrics.wrapper_call_success = exist(output_nii, 'file') == 2;
                result.metrics.call_pattern = 'rest_Smooth(input_nii, output_nii, fwhm)';
            catch ME1
                result.warnings{end+1} = ['rest_Smooth call pattern failed: ', ME1.message];
                error('rest_Smooth subject wrapper failed. Manual signature review required.');
            end
        end

        if ~exist(output_nii, 'file')
            error('DPABI subject wrapper did not produce output NIfTI.');
        end

        result.outputs{end+1} = output_nii;
        result.metrics.output_exists = exist(output_nii, 'file') == 2;

    catch ME
        result.ok = false;
        try
            result.errors{end+1} = getReport(ME, 'extended', 'hyperlinks', 'off');
        catch
            result.errors{end+1} = ME.message;
        end
    end

    fid = fopen(output_json, 'w');
    if fid == -1
        error(['Cannot open output JSON for writing: ', output_json]);
    end

    fwrite(fid, jsonencode(result), 'char');
    fclose(fid);

    if ~result.ok
        exit(1);
    end
end
3. 创建 backend/app/tools/dpabi_subject_wrapper.py

创建文件：

backend/app/tools/dpabi_subject_wrapper.py

目标：对单个 subject 运行 DPABI smoothing wrapper。

提供函数：

run_dpabi_subject_smooth(
    matlab_command: str,
    dpabi_dir: str,
    subject_id: str,
    input_bold: str,
    derivatives_dir: str,
    work_dir: str,
    log_dir: str,
    function_name: str = "y_Smooth",
    fwhm: list[float] | None = None,
    approved: bool = False,
    matlab_script_dir: str = "./matlab",
) -> dict

要求：

approved=false 时安全失败。
function_name 只允许 y_Smooth / rest_Smooth。
读取 wrapper contracts，确认 wrapper_candidate=true。
只允许 synthetic BIDS-like 路径：
路径中必须包含 examples/synthetic_bids/rawdata
否则拒绝执行
如果 input 是 .nii.gz：
使用 nibabel 读取并写成 work/dpabi/subject_wrapper_workspace/{subject_id}/input.nii
不修改原始输入
调用 MATLAB wrapper。
输出：
derivatives/dpabi_single_function/{subject_id}/func/{subject_id}_dpabi_smooth.nii
derivatives/dpabi_single_function/{subject_id}/func/dpabi_subject_wrapper_result.json
日志：
logs/{subject_id}_dpabi_subject_wrapper_stdout.log
logs/{subject_id}_dpabi_subject_wrapper_stderr.log
不使用 shell=True。
不调用 DPARSF_run / DPARSFA_run。

参考实现方向：

from __future__ import annotations

import json
import subprocess
from pathlib import Path
from typing import Any


ALLOWLISTED_SINGLE_FUNCTIONS = {"y_Smooth", "rest_Smooth"}


def _matlab_quote(value: str) -> str:
    return value.replace("'", "''")


def _read_json(path: Path) -> dict[str, Any] | None:
    if not path.exists():
        return None
    try:
        return json.loads(path.read_text(encoding="utf-8"))
    except Exception:
        return None


def _find_contract(contracts: dict[str, Any], function_name: str) -> dict[str, Any] | None:
    for item in contracts.get("contracts", []):
        if item.get("function_name") == function_name:
            return item
    return None


def _prepare_input_nifti(input_bold: str, subject_id: str, work_dir: str) -> str:
    input_path = Path(input_bold)
    workspace = Path(work_dir) / "dpabi" / "subject_wrapper_workspace" / subject_id
    workspace.mkdir(parents=True, exist_ok=True)

    output_path = workspace / "input.nii"

    if input_path.suffix == ".nii":
        try:
            import shutil
            shutil.copyfile(input_path, output_path)
            return str(output_path)
        except Exception as exc:
            raise RuntimeError(f"Failed to copy input NIfTI: {exc}") from exc

    try:
        import nibabel as nib
    except ImportError as exc:
        raise RuntimeError("Missing dependency: nibabel. Install with: pip install nibabel") from exc

    img = nib.load(str(input_path))
    nib.save(img, str(output_path))
    return str(output_path)


def run_dpabi_subject_smooth(
    matlab_command: str,
    dpabi_dir: str,
    subject_id: str,
    input_bold: str,
    derivatives_dir: str,
    work_dir: str,
    log_dir: str,
    function_name: str = "y_Smooth",
    fwhm: list[float] | None = None,
    approved: bool = False,
    matlab_script_dir: str = "./matlab",
) -> dict[str, Any]:
    if not approved:
        return {
            "ok": False,
            "node_id": "dpabi_subject_smooth",
            "backend": "matlab-dpabi",
            "subject_id": subject_id,
            "outputs": [],
            "warnings": [],
            "errors": ["DPABI subject wrapper requires approved=true."],
        }

    if function_name not in ALLOWLISTED_SINGLE_FUNCTIONS:
        return {
            "ok": False,
            "node_id": "dpabi_subject_smooth",
            "backend": "matlab-dpabi",
            "subject_id": subject_id,
            "outputs": [],
            "warnings": [],
            "errors": [f"Function is not allowlisted: {function_name}"],
        }

    normalized_input = str(input_bold).replace("\\", "/")
    if "examples/synthetic_bids/rawdata" not in normalized_input:
        return {
            "ok": False,
            "node_id": "dpabi_subject_smooth",
            "backend": "matlab-dpabi",
            "subject_id": subject_id,
            "outputs": [],
            "warnings": [],
            "errors": [
                "Refusing to run DPABI subject wrapper on non-synthetic input. "
                f"Input was: {input_bold}"
            ],
        }

    contracts_path = Path(work_dir) / "dpabi" / "dpabi_wrapper_contracts.json"
    contracts = _read_json(contracts_path)
    if not contracts:
        return {
            "ok": False,
            "node_id": "dpabi_subject_smooth",
            "backend": "matlab-dpabi",
            "subject_id": subject_id,
            "outputs": [],
            "warnings": [],
            "errors": [f"Missing wrapper contracts: {contracts_path}"],
        }

    contract = _find_contract(contracts, function_name)
    if not contract or not contract.get("wrapper_candidate"):
        return {
            "ok": False,
            "node_id": "dpabi_subject_smooth",
            "backend": "matlab-dpabi",
            "subject_id": subject_id,
            "outputs": [],
            "warnings": [],
            "errors": [
                f"Function is not a wrapper candidate: {function_name}. "
                f"Contract: {contract}"
            ],
        }

    fwhm = fwhm or [4, 4, 4]

    out_dir = Path(derivatives_dir) / "dpabi_single_function" / subject_id / "func"
    out_dir.mkdir(parents=True, exist_ok=True)

    output_nii = out_dir / f"{subject_id}_dpabi_smooth.nii"
    result_json = out_dir / "dpabi_subject_wrapper_result.json"

    log_path = Path(log_dir)
    log_path.mkdir(parents=True, exist_ok=True)
    stdout_log = log_path / f"{subject_id}_dpabi_subject_wrapper_stdout.log"
    stderr_log = log_path / f"{subject_id}_dpabi_subject_wrapper_stderr.log"

    try:
        prepared_input = _prepare_input_nifti(input_bold, subject_id, work_dir)
    except Exception as exc:
        return {
            "ok": False,
            "node_id": "dpabi_subject_smooth",
            "backend": "matlab-dpabi",
            "subject_id": subject_id,
            "outputs": [],
            "warnings": [],
            "errors": [str(exc)],
        }

    matlab_script_path = str(Path(matlab_script_dir).resolve())

    matlab_code = (
        "try, "
        f"addpath('{_matlab_quote(matlab_script_path)}'); "
        f"dpabi_subject_smooth_wrapper('{_matlab_quote(str(Path(dpabi_dir).resolve()))}', "
        f"'{_matlab_quote(function_name)}', "
        f"'{_matlab_quote(str(Path(prepared_input).resolve()))}', "
        f"'{_matlab_quote(str(output_nii.resolve()))}', "
        f"'{_matlab_quote(json.dumps(fwhm))}', "
        f"'{_matlab_quote(str(result_json.resolve()))}'); "
        "catch ME, disp(getReport(ME)); exit(1); end; exit(0);"
    )

    cmd = [
        matlab_command,
        "-nodisplay",
        "-nosplash",
        "-r",
        matlab_code,
    ]

    with stdout_log.open("w", encoding="utf-8") as out, stderr_log.open("w", encoding="utf-8") as err:
        completed = subprocess.run(cmd, stdout=out, stderr=err, check=False)

    if result_json.exists():
        data = _read_json(result_json) or {
            "ok": False,
            "errors": ["Failed to parse DPABI subject wrapper result JSON."],
        }
    else:
        data = {
            "ok": False,
            "errors": ["DPABI subject wrapper did not produce result JSON."],
        }

    data["node_id"] = "dpabi_subject_smooth"
    data["backend"] = "matlab-dpabi"
    data["subject_id"] = subject_id
    data["function_name"] = function_name
    data["returncode"] = completed.returncode
    data["stdout_log"] = str(stdout_log)
    data["stderr_log"] = str(stderr_log)
    data["result_json"] = str(result_json)
    data["prepared_input"] = prepared_input

    if completed.returncode != 0:
        data["ok"] = False
        data.setdefault("errors", [])
        data["errors"].append(f"MATLAB exited with return code {completed.returncode}.")

    outputs = list(data.get("outputs", []))
    outputs.extend([str(output_nii), str(result_json)])
    data["outputs"] = sorted(set(outputs))

    return data
4. 创建 backend/app/tools/dpabi_subject_wrapper_report.py

创建文件：

backend/app/tools/dpabi_subject_wrapper_report.py

目标：聚合 subject-level DPABI wrapper 结果，生成 summary/report。

提供函数：

write_dpabi_subject_wrapper_report(
    derivatives_dir: str,
    report_dir: str,
) -> dict

输出：

reports/dpabi/dpabi_subject_wrapper_summary.json
reports/dpabi/dpabi_subject_wrapper_report.md

要求：

搜索：
derivatives/dpabi_single_function/*/func/dpabi_subject_wrapper_result.json
汇总：
subjects_total
subjects_success
subjects_failed
function_name count
warnings/errors
可选对比 SPM 输出：
如果 derivatives/spm/{subject_id}/func/ 中存在 SPM smoothing 输出，则用 nibabel 计算：
shape_match
mean_abs_diff
max_abs_diff
如果不存在，只记录 warning
不做科学结论，只做工程差异对比。

参考实现：

from __future__ import annotations

import json
from pathlib import Path
from typing import Any


def _read_json(path: Path) -> dict[str, Any] | None:
    if not path.exists():
        return None
    try:
        return json.loads(path.read_text(encoding="utf-8"))
    except Exception:
        return None


def _compare_nifti(a_path: Path, b_path: Path) -> dict[str, Any]:
    try:
        import nibabel as nib
        import numpy as np
    except ImportError:
        return {
            "ok": False,
            "error": "Missing nibabel or numpy.",
        }

    if not a_path.exists() or not b_path.exists():
        return {
            "ok": False,
            "error": "One or both NIfTI files do not exist.",
        }

    a = nib.load(str(a_path)).get_fdata(dtype="float32")
    b = nib.load(str(b_path)).get_fdata(dtype="float32")

    if a.shape != b.shape:
        return {
            "ok": False,
            "shape_match": False,
            "a_shape": list(a.shape),
            "b_shape": list(b.shape),
        }

    diff = np.abs(a - b)

    return {
        "ok": True,
        "shape_match": True,
        "mean_abs_diff": float(np.mean(diff)),
        "max_abs_diff": float(np.max(diff)),
    }


def _find_spm_smooth_output(derivatives_dir: Path, subject_id: str) -> Path | None:
    candidates = list((derivatives_dir / "spm" / subject_id / "func").glob("*.nii"))
    if candidates:
        return candidates[0]
    candidates = list((derivatives_dir / "spm" / subject_id / "func").glob("*.nii.gz"))
    if candidates:
        return candidates[0]
    return None


def write_dpabi_subject_wrapper_report(
    derivatives_dir: str,
    report_dir: str,
) -> dict[str, Any]:
    derivatives = Path(derivatives_dir)
    out_dir = Path(report_dir) / "dpabi"
    out_dir.mkdir(parents=True, exist_ok=True)

    summary_path = out_dir / "dpabi_subject_wrapper_summary.json"
    report_path = out_dir / "dpabi_subject_wrapper_report.md"

    result_paths = sorted(
        (derivatives / "dpabi_single_function").glob("*/func/dpabi_subject_wrapper_result.json")
    )

    subjects = []
    warnings: list[str] = []
    errors: list[str] = []
    function_counts: dict[str, int] = {}

    for path in result_paths:
        payload = _read_json(path)
        if not payload:
            warnings.append(f"Invalid result JSON: {path}")
            continue

        subject_id = payload.get("subject_id") or path.parents[1].name
        function_name = payload.get("function_name", "unknown")
        function_counts[function_name] = function_counts.get(function_name, 0) + 1

        outputs = payload.get("outputs", [])
        dpabi_output = None
        for item in outputs:
            if str(item).endswith("_dpabi_smooth.nii"):
                dpabi_output = Path(item)
                break

        spm_output = _find_spm_smooth_output(derivatives, subject_id)
        comparison = None

        if dpabi_output and spm_output:
            comparison = _compare_nifti(dpabi_output, spm_output)
        else:
            comparison = {
                "ok": False,
                "error": "SPM baseline output not found or DPABI output missing.",
            }

        subjects.append({
            "subject_id": subject_id,
            "ok": payload.get("ok"),
            "function_name": function_name,
            "dpabi_output": str(dpabi_output) if dpabi_output else None,
            "spm_output": str(spm_output) if spm_output else None,
            "comparison_to_spm": comparison,
            "warnings": payload.get("warnings", []),
            "errors": payload.get("errors", []),
        })

    subjects_total = len(subjects)
    subjects_success = sum(1 for item in subjects if item.get("ok"))
    subjects_failed = subjects_total - subjects_success

    summary = {
        "ok": subjects_failed == 0 and subjects_total > 0,
        "subjects_total": subjects_total,
        "subjects_success": subjects_success,
        "subjects_failed": subjects_failed,
        "function_counts": function_counts,
        "subjects": subjects,
        "warnings": warnings,
        "errors": errors,
        "note": "SPM comparison is engineering-only and not a scientific validation.",
    }

    summary_path.write_text(
        json.dumps(summary, ensure_ascii=False, indent=2),
        encoding="utf-8",
    )

    lines = []
    lines.append("# DPABI Subject Wrapper Report")
    lines.append("")
    lines.append("## Summary")
    lines.append("")
    lines.append(f"- Subjects total: {subjects_total}")
    lines.append(f"- Subjects success: {subjects_success}")
    lines.append(f"- Subjects failed: {subjects_failed}")
    lines.append(f"- Function counts: `{function_counts}`")
    lines.append("")
    lines.append("## Subject Results")
    lines.append("")
    if subjects:
        lines.append("| Subject | OK | Function | DPABI Output | SPM Baseline | Mean Abs Diff | Max Abs Diff |")
        lines.append("|---|---:|---|---|---|---:|---:|")
        for item in subjects:
            comp = item.get("comparison_to_spm") or {}
            lines.append(
                f"| {item.get('subject_id')} | {item.get('ok')} | {item.get('function_name')} | "
                f"`{item.get('dpabi_output')}` | `{item.get('spm_output')}` | "
                f"{comp.get('mean_abs_diff')} | {comp.get('max_abs_diff')} |"
            )
    else:
        lines.append("No DPABI subject wrapper results found.")
    lines.append("")
    lines.append("## Safety Note")
    lines.append("")
    lines.append("This report is for engineering integration only. It is not a clinical or scientific validation.")

    report_path.write_text("\n".join(lines) + "\n", encoding="utf-8")

    return {
        "ok": True,
        "node_id": "dpabi_subject_wrapper_report",
        "backend": "python",
        "outputs": [str(summary_path), str(report_path)],
        "metrics": {
            "subjects_total": subjects_total,
            "subjects_success": subjects_success,
            "subjects_failed": subjects_failed,
        },
        "warnings": warnings,
        "errors": errors,
    }
5. 修改 backend/app/runtime/node_registry.py

新增两个节点：

dpabi_subject_smooth
dpabi_subject_wrapper_report

新增导入：

from backend.app.tools.dpabi_subject_wrapper import run_dpabi_subject_smooth
from backend.app.tools.dpabi_subject_wrapper_report import write_dpabi_subject_wrapper_report

新增 runner：

def run_dpabi_subject_smooth_node(
    context: NodeExecutionContext,
    node: PipelineNode,
) -> dict[str, Any]:
    if not context.subject_id or not context.subject_record:
        return {
            "ok": False,
            "node_id": node.id,
            "backend": "matlab-dpabi",
            "outputs": [],
            "errors": ["Missing subject_id or subject_record in context."],
        }

    bold = None
    for session in context.subject_record.get("sessions", []):
        for func in session.get("func", []):
            if func.get("bold"):
                bold = func.get("bold")
                break
        if bold:
            break

    if not bold:
        return {
            "ok": False,
            "node_id": node.id,
            "backend": "matlab-dpabi",
            "subject_id": context.subject_id,
            "outputs": [],
            "errors": ["No BOLD input found for subject."],
        }

    result = run_dpabi_subject_smooth(
        matlab_command=context.matlab_command,
        dpabi_dir=context.dpabi_dir,
        subject_id=context.subject_id,
        input_bold=bold,
        derivatives_dir=context.derivatives_dir,
        work_dir=context.work_dir,
        log_dir=context.log_dir,
        function_name=node.params.get("function_name", "y_Smooth"),
        fwhm=node.params.get("fwhm", [4, 4, 4]),
        approved=bool(node.params.get("approved", False)),
        matlab_script_dir="./matlab",
    )

    result["node_id"] = node.id
    return result


def run_dpabi_subject_wrapper_report_node(
    context: NodeExecutionContext,
    node: PipelineNode,
) -> dict[str, Any]:
    result = write_dpabi_subject_wrapper_report(
        derivatives_dir=context.derivatives_dir,
        report_dir=context.project_config.get("runtime", {}).get("report_dir", "./reports"),
    )
    result["node_id"] = node.id
    return result

更新 NODE_REGISTRY：

"dpabi_subject_smooth": run_dpabi_subject_smooth_node,
"dpabi_subject_wrapper_report": run_dpabi_subject_wrapper_report_node,
6. 创建 examples/pipeline_dpabi_subject_wrapper.yaml

创建文件：

examples/pipeline_dpabi_subject_wrapper.yaml

内容：

pipeline_id: dpabi_subject_wrapper_pipeline
version: "0.1.0"
modality: synthetic-rsfmri
description: "Run approved DPABI single-function smoothing wrapper on synthetic subjects and compare with SPM baseline."

execution:
  stop_on_failure: true
  run_id: "run_dpabi_subject_wrapper_001"
  scheduler:
    mode: "local_parallel"
    max_workers: 2
    matlab_max_workers: 1

nodes:
  - id: create_synthetic_bids
    name: Create Synthetic BIDS Dataset
    agent: data-inspector
    backend: python
    depends_on: []
    inputs: []
    outputs:
      - "./examples/synthetic_bids/rawdata/dataset_description.json"
      - "./examples/synthetic_bids/rawdata/participants.tsv"
    params:
      output_dir: "./examples/synthetic_bids/rawdata"
      subjects:
        - sub-001
        - sub-002
    parallel_level: project
    gpu_supported: false
    cache: false

  - id: data_inspection
    name: Data Inspection
    agent: data-inspector
    backend: python
    depends_on:
      - create_synthetic_bids
    inputs:
      - "./examples/synthetic_bids/rawdata"
    outputs:
      - "./work/dataset_index/dataset_index.json"
      - "./work/dataset_index/data_completeness_report.json"
      - "./work/dataset_index/subject_table.csv"
    params:
      rawdata_dir: "./examples/synthetic_bids/rawdata"
      output_dir: "./work/dataset_index"
      read_nifti_metadata: true
    parallel_level: project
    gpu_supported: false
    cache: false

  - id: environment_check
    name: Environment Check
    agent: system
    backend: matlab
    depends_on: []
    inputs: []
    outputs:
      - "./work/environment_check.json"
    params: {}
    parallel_level: project
    gpu_supported: false
    cache: false

  - id: dpabi_signature_probe
    name: DPABI Signature Probe
    agent: dpabi-runner
    backend: matlab-dpabi
    depends_on:
      - environment_check
    inputs: []
    outputs:
      - "./work/dpabi/dpabi_function_signatures.json"
    params: {}
    parallel_level: project
    gpu_supported: false
    cache: false

  - id: dpabi_wrapper_contracts
    name: DPABI Wrapper Contracts
    agent: dpabi-runner
    backend: python
    depends_on:
      - dpabi_signature_probe
    inputs:
      - "./work/dpabi/dpabi_function_signatures.json"
    outputs:
      - "./work/dpabi/dpabi_wrapper_contracts.json"
      - "./work/dpabi/dpabi_wrapper_contracts.yaml"
      - "./reports/dpabi/dpabi_signature_probe_report.md"
    params:
      signatures_path: "./work/dpabi/dpabi_function_signatures.json"
    parallel_level: project
    gpu_supported: false
    cache: false

  - id: spm_smooth_subject
    name: SPM Smooth Subject BOLD Baseline
    agent: spm-runner
    backend: matlab-spm
    depends_on:
      - data_inspection
    inputs:
      - "./work/dataset_index/dataset_index.json"
    outputs: []
    params:
      dataset_index: "./work/dataset_index/dataset_index.json"
      fwhm: [4, 4, 4]
    parallel_level: subject
    gpu_supported: false
    cache: false

  - id: dpabi_subject_smooth
    name: Approved DPABI Subject Smooth
    agent: dpabi-runner
    backend: matlab-dpabi
    depends_on:
      - data_inspection
      - dpabi_wrapper_contracts
    inputs:
      - "./work/dataset_index/dataset_index.json"
      - "./work/dpabi/dpabi_wrapper_contracts.json"
    outputs: []
    params:
      function_name: "y_Smooth"
      fwhm: [4, 4, 4]
      approved: false
    parallel_level: subject
    gpu_supported: false
    cache: false

  - id: dpabi_subject_wrapper_report
    name: DPABI Subject Wrapper Report
    agent: dpabi-runner
    backend: python
    depends_on:
      - dpabi_subject_smooth
      - spm_smooth_subject
    inputs: []
    outputs:
      - "./reports/dpabi/dpabi_subject_wrapper_summary.json"
      - "./reports/dpabi/dpabi_subject_wrapper_report.md"
    params: {}
    parallel_level: project
    gpu_supported: false
    cache: false

注意：默认 dpabi_subject_smooth.params.approved=false，直接运行应该安全失败。真正执行必须通过 CLI/API 显式 approval。

7. 创建 backend/app/tools/run_dpabi_subject_wrapper_cli.py

创建文件：

backend/app/tools/run_dpabi_subject_wrapper_cli.py

功能：

默认 project config：
examples/project_config_dataset.yaml
默认 pipeline：
examples/pipeline_dpabi_subject_wrapper.yaml
默认 function_name：
y_Smooth
默认不 approved。
如果传入 --approve，生成 approved pipeline 副本：
work/dpabi/approved_pipeline_dpabi_subject_wrapper.yaml

并把：

dpabi_subject_smooth.params.approved=true
dpabi_subject_smooth.params.function_name=function_name

参考实现：

from __future__ import annotations

import json
import sys
from pathlib import Path

from backend.app.runtime.pipeline_executor import run_pipeline


def _make_approved_pipeline_copy(source: Path, target: Path, function_name: str) -> Path:
    try:
        import yaml
    except ImportError as exc:
        raise RuntimeError("Missing dependency: PyYAML. Install with: pip install pyyaml") from exc

    data = yaml.safe_load(source.read_text(encoding="utf-8"))

    for node in data.get("nodes", []):
        if node.get("id") == "dpabi_subject_smooth":
            node.setdefault("params", {})
            node["params"]["approved"] = True
            node["params"]["function_name"] = function_name

    target.parent.mkdir(parents=True, exist_ok=True)
    target.write_text(yaml.safe_dump(data, sort_keys=False), encoding="utf-8")
    return target


def main() -> int:
    args = sys.argv[1:]
    approved = "--approve" in args
    args = [arg for arg in args if arg != "--approve"]

    project_config = Path(args[0]) if len(args) > 0 else Path("examples/project_config_dataset.yaml")
    pipeline = Path(args[1]) if len(args) > 1 else Path("examples/pipeline_dpabi_subject_wrapper.yaml")
    function_name = args[2] if len(args) > 2 else "y_Smooth"

    if approved:
        pipeline = _make_approved_pipeline_copy(
            source=pipeline,
            target=Path("outputs/work/dpabi/approved_pipeline_dpabi_subject_wrapper.yaml"),
            function_name=function_name,
        )

    summary = run_pipeline(project_config, pipeline)
    print(json.dumps(summary, ensure_ascii=False, indent=2))

    status = summary.get("status")
    if status == "SUCCESS":
        return 0
    if status == "INVALID":
        return 1
    return 2


if __name__ == "__main__":
    raise SystemExit(main())
8. 修改 backend/app/api/models.py

新增 request model：

class DpabiSubjectWrapperRequest(BaseModel):
    project_config_path: str = Field(default="examples/project_config_dataset.yaml")
    pipeline_path: str = Field(default="examples/pipeline_dpabi_subject_wrapper.yaml")
    function_name: str = Field(default="y_Smooth")
    approved: bool = Field(default=False)
9. 修改 backend/app/api/routes.py

新增 API：

POST /api/dpabi/subject-wrapper/run
GET  /api/dpabi/subject-wrapper

新增导入：

from backend.app.api.models import DpabiSubjectWrapperRequest
from backend.app.runtime.pipeline_executor import run_pipeline

新增辅助函数：

def _make_dpabi_subject_wrapper_approved_copy(
    source: Path,
    target: Path,
    function_name: str,
) -> Path:
    import yaml

    data = yaml.safe_load(source.read_text(encoding="utf-8"))

    for node in data.get("nodes", []):
        if node.get("id") == "dpabi_subject_smooth":
            node.setdefault("params", {})
            node["params"]["approved"] = True
            node["params"]["function_name"] = function_name

    target.parent.mkdir(parents=True, exist_ok=True)
    target.write_text(yaml.safe_dump(data, sort_keys=False), encoding="utf-8")
    return target

新增路由：

@router.post("/api/dpabi/subject-wrapper/run")
def api_run_dpabi_subject_wrapper(request: DpabiSubjectWrapperRequest) -> dict[str, Any]:
    if not request.approved:
        raise HTTPException(
            status_code=403,
            detail="DPABI subject wrapper execution requires approved=true.",
        )

    if request.function_name not in {"y_Smooth", "rest_Smooth"}:
        raise HTTPException(
            status_code=400,
            detail="Only y_Smooth and rest_Smooth are allowlisted in this MVP.",
        )

    try:
        approved_pipeline = _make_dpabi_subject_wrapper_approved_copy(
            source=Path(request.pipeline_path),
            target=Path("outputs/work/dpabi/approved_pipeline_dpabi_subject_wrapper.yaml"),
            function_name=request.function_name,
        )

        summary = run_pipeline(
            request.project_config_path,
            str(approved_pipeline),
        )

        if summary.get("status") not in {"SUCCESS", "PARTIAL"}:
            raise HTTPException(status_code=400, detail=summary)

        return summary

    except HTTPException:
        raise
    except Exception as exc:
        raise HTTPException(status_code=400, detail=str(exc))


@router.get("/api/dpabi/subject-wrapper")
def api_get_dpabi_subject_wrapper() -> dict[str, Any]:
    base = Path("reports") / "dpabi"

    return {
        "ok": True,
        "summary": _read_json_if_exists(base / "dpabi_subject_wrapper_summary.json"),
        "report": _read_text_if_exists(base / "dpabi_subject_wrapper_report.md"),
    }

同时修改已有 /api/dpabi/capabilities，新增返回：

"subject_wrapper_summary": _read_json_if_exists(Path("reports") / "dpabi" / "dpabi_subject_wrapper_summary.json"),

修改已有 /api/reports/dpabi，新增返回：

"subject_wrapper_report": _read_text_if_exists(base / "dpabi_subject_wrapper_report.md"),
10. 修改 frontend/src/api.ts

新增：

export async function runDpabiSubjectWrapper(
  baseUrl: string,
  payload: {
    project_config_path: string;
    pipeline_path: string;
    function_name: string;
    approved: boolean;
  }
) {
  return requestJson<Record<string, unknown>>(
    baseUrl,
    "/api/dpabi/subject-wrapper/run",
    {
      method: "POST",
      body: JSON.stringify(payload)
    }
  );
}

export async function getDpabiSubjectWrapper(baseUrl: string) {
  return requestJson<Record<string, unknown>>(
    baseUrl,
    "/api/dpabi/subject-wrapper"
  );
}
11. 修改 frontend/src/components/DpabiPanel.tsx

新增导入：

import {
  runDpabiSubjectWrapper,
  getDpabiSubjectWrapper
} from "../api";

如果已有多个 DPABI API import，请合并，不要重复 import。

新增 state：

const [subjectWrapperFunction, setSubjectWrapperFunction] = useState("y_Smooth");
const [subjectWrapperResult, setSubjectWrapperResult] = useState<Record<string, unknown> | null>(null);
const [subjectWrapperStatus, setSubjectWrapperStatus] = useState("IDLE");

新增函数：

async function handleRunSubjectWrapper() {
  const confirmed = window.confirm(
    `确认运行 DPABI subject-level wrapper：${subjectWrapperFunction}？这只处理 synthetic BIDS 数据，不会运行完整 DPABI/DPARSF。`
  );

  if (!confirmed) return;

  setSubjectWrapperStatus("RUNNING");
  setError("");

  try {
    const result = await runDpabiSubjectWrapper(baseUrl, {
      project_config_path: "examples/project_config_dataset.yaml",
      pipeline_path: "examples/pipeline_dpabi_subject_wrapper.yaml",
      function_name: subjectWrapperFunction,
      approved: true
    });
    setSubjectWrapperResult(result);
    setSubjectWrapperStatus("SUCCESS");
  } catch (err) {
    setError(err instanceof Error ? err.message : String(err));
    setSubjectWrapperStatus("ERROR");
  }
}

async function handleLoadSubjectWrapper() {
  setError("");

  try {
    const result = await getDpabiSubjectWrapper(baseUrl);
    setSubjectWrapperResult(result);
  } catch (err) {
    setError(err instanceof Error ? err.message : String(err));
  }
}

在 JSX 中新增：

<h3>Approved DPABI Subject-Level Wrapper</h3>

<div className="formGrid">
  <label>
    Function
    <select
      value={subjectWrapperFunction}
      onChange={(event) => setSubjectWrapperFunction(event.target.value)}
    >
      <option value="y_Smooth">y_Smooth</option>
      <option value="rest_Smooth">rest_Smooth</option>
    </select>
  </label>
</div>

<div className="row">
  <button className="dangerButton" onClick={handleRunSubjectWrapper}>
    批准并运行 Subject Wrapper
  </button>
  <button onClick={handleLoadSubjectWrapper}>
    加载 Subject Wrapper 结果
  </button>
  <StatusBadge status={subjectWrapperStatus} />
</div>

<JsonBlock value={subjectWrapperResult} emptyText="尚未运行 subject wrapper" />

<h3>DPABI Subject Wrapper Summary</h3>
<JsonBlock
  value={capabilities?.subject_wrapper_summary}
  emptyText="暂无 subject wrapper summary"
/>

<h3>DPABI Subject Wrapper Report</h3>
<TextViewer
  text={
    typeof report?.subject_wrapper_report === "string"
      ? report.subject_wrapper_report
      : null
  }
  emptyText="暂无 subject wrapper report"
/>

确保 styles.css 中已经有 select 样式；如果没有，追加：

select {
  border: 1px solid #d2d8e8;
  border-radius: 10px;
  padding: 9px 12px;
  min-width: 280px;
  background: white;
}
12. 修改 backend/app/tools/api_smoke_test.py

新增只读测试：

call("GET", "/api/dpabi/subject-wrapper")

不要在 smoke test 中调用 POST /api/dpabi/subject-wrapper/run，避免误启动 MATLAB。

13. 更新 README.md

追加第二十三步说明：

## Step 23: DPABI Subject-Level Single-Function Wrapper

This step adds an approved subject-level DPABI single-function wrapper.

It only processes synthetic BIDS-like data.

Supported MVP functions:

- y_Smooth
- rest_Smooth

### Run without approval

```bash
python -m backend.app.tools.run_dpabi_subject_wrapper_cli

This should fail safely because approval is missing.

Run with approval
python -m backend.app.tools.run_dpabi_subject_wrapper_cli examples/project_config_dataset.yaml examples/pipeline_dpabi_subject_wrapper.yaml y_Smooth --approve

Expected outputs:

derivatives/dpabi_single_function/sub-001/func/sub-001_dpabi_smooth.nii
derivatives/dpabi_single_function/sub-001/func/dpabi_subject_wrapper_result.json
reports/dpabi/dpabi_subject_wrapper_summary.json
reports/dpabi/dpabi_subject_wrapper_report.md
work/pipeline_runs/run_dpabi_subject_wrapper_001/summary.json
API

Run approved subject wrapper:

curl -X POST http://127.0.0.1:8000/api/dpabi/subject-wrapper/run \
  -H "Content-Type: application/json" \
  -d '{
    "project_config_path": "examples/project_config_dataset.yaml",
    "pipeline_path": "examples/pipeline_dpabi_subject_wrapper.yaml",
    "function_name": "y_Smooth",
    "approved": true
  }'

Read result:

curl http://127.0.0.1:8000/api/dpabi/subject-wrapper
Frontend

Use the DPABI panel and choose:

Approved DPABI Subject-Level Wrapper
Safety

This step does not:

run DPARSF_run
run DPARSFA_run
call DPABI GUI
process real medical imaging data
modify rawdata
modify DPABI source
delete files

---

## 14. 验收标准

完成后确认新增或修改了这些文件：

```text
specs/dpabi_subject_wrapper_spec.md
matlab/dpabi_subject_smooth_wrapper.m
backend/app/tools/dpabi_subject_wrapper.py
backend/app/tools/dpabi_subject_wrapper_report.py
backend/app/runtime/node_registry.py
examples/pipeline_dpabi_subject_wrapper.yaml
backend/app/tools/run_dpabi_subject_wrapper_cli.py
backend/app/api/models.py
backend/app/api/routes.py
backend/app/tools/api_smoke_test.py
frontend/src/api.ts
frontend/src/components/DpabiPanel.tsx
frontend/src/styles.css
README.md

先运行不带 approval：

python -m backend.app.tools.run_dpabi_subject_wrapper_cli

应该安全失败，不能执行 DPABI subject wrapper。

然后运行 approved subject wrapper：

python -m backend.app.tools.run_dpabi_subject_wrapper_cli examples/project_config_dataset.yaml examples/pipeline_dpabi_subject_wrapper.yaml y_Smooth --approve

如果本地 DPABI 中存在 y_Smooth 且 contract 是 wrapper_candidate，应生成：

derivatives/dpabi_single_function/sub-001/func/sub-001_dpabi_smooth.nii
derivatives/dpabi_single_function/sub-001/func/dpabi_subject_wrapper_result.json
reports/dpabi/dpabi_subject_wrapper_summary.json
reports/dpabi/dpabi_subject_wrapper_report.md
work/pipeline_runs/run_dpabi_subject_wrapper_001/summary.json

其中 subject result JSON 应包含：

{
  "node_id": "dpabi_subject_smooth",
  "backend": "matlab-dpabi",
  "subject_id": "sub-001",
  "function_name": "y_Smooth"
}

其中 summary 应包含：

{
  "subjects_total": 2,
  "subjects_success": 2,
  "function_counts": {
    "y_Smooth": 2
  }
}

如果 y_Smooth 不存在或 contract 不是 wrapper_candidate：

必须安全失败。
不应读取真实数据。
不应调用 DPARSF_run。
不应调用 DPABI GUI。
不应修改 rawdata。
错误应写入 summary / state / logs。

启动后端：

uvicorn backend.app.main:app --reload --host 127.0.0.1 --port 8000

测试 API：

curl http://127.0.0.1:8000/api/dpabi/subject-wrapper

未批准 POST 应失败：

curl -X POST http://127.0.0.1:8000/api/dpabi/subject-wrapper/run \
  -H "Content-Type: application/json" \
  -d '{"function_name": "y_Smooth", "approved": false}'

应返回 403 或清晰错误。

批准 POST 可运行：

curl -X POST http://127.0.0.1:8000/api/dpabi/subject-wrapper/run \
  -H "Content-Type: application/json" \
  -d '{"function_name": "y_Smooth", "approved": true}'

启动前端：

cd frontend
npm run dev

页面应该能完成：

DPABI Panel 显示 Subject-Level Wrapper 区域。
可以选择 y_Smooth 或 rest_Smooth。
点击运行前有 confirm 弹窗。
approved 后调用后端运行 subject wrapper。
显示 pipeline summary。
显示 subject wrapper summary。
显示 subject wrapper report。
不运行完整 DPABI preprocessing。
不调用 DPARSF_run / DPARSFA_run。
不读取或修改真实 rawdata。
15. 重要限制

本步骤只做 DPABI subject-level single-function wrapper。

不要实现：

DPABI 全流程执行
DPARSF_run 自动执行
DPARSFA_run 自动执行
DPABI GUI 自动化
真实医学影像处理
自动参数优化
修改 rawdata
修改 DPABI 源码
删除文件
并行 DPABI 批处理
GPU DPABI

完成后请总结：

新增了哪些文件
修改了哪些文件
如何运行不带 approval 的安全失败测试
如何运行 approved subject wrapper
subject wrapper 与 sandbox wrapper 的区别是什么
subject wrapper report 如何与 SPM baseline 做工程对比
为什么这一步仍然不是完整 DPABI pipeline execution

'''
## Step 23 主要做的是什么
第二十三步目标：实现"DPABI Single-Function Subject Wrapper + SPM Baseline Comparison 闭环"

### 核心目的
在前一步（Step 22）已经在 synthetic sandbox 中测试了 y_Smooth 和 rest_Smooth 的基础上， 这一步将 DPABI 单函数 wrapper 真正接入 subject-level pipeline，对真实 subject 数据执行 smoothing，并可选择与 SPM smoothing 结果做工程对比 。

### 具体做的事情
方面 说明 读取 Subject 数据 从 synthetic BIDS-like dataset_index.json 获取 COMPLETE subject 的 BOLD 输入 输入安全处理 只允许 examples/synthetic_bids/rawdata 路径，自动转换 .nii.gz 到 .nii Subject-Level 执行 对每个 subject 调用 allowlisted DPABI 函数（y_Smooth / rest_Smooth） 输出到 Derivatives 结果写入 derivatives/dpabi_single_function/{subject_id}/func/ 合约验证 执行前验证函数在 contracts 中且是 wrapper_candidate SPM 对比 可选与 SPM smoothing 输出做工程差异对比（shape_match, mean_abs_diff, max_abs_diff） 数据集级报告 聚合所有 subject 结果，生成 summary JSON 和 Markdown report

### 关键安全机制
- ✅ 输入路径检查 - 必须包含 examples/synthetic_bids/rawdata
- ✅ 审批门控 - 需要 approved=true
- ✅ 合约验证 - 函数必须是 wrapper_candidate
- ✅ 不修改 rawdata - 只读取，工作副本放在 workspace
- ❌ 不运行完整 DPABI pipeline
- ❌ 不调用 DPARSF_run / DPARSFA_run
- ❌ 不调用 DPABI GUI
### 输出产物
```
derivatives/dpabi_single_function/
└── {subject_id}/
    └── func/
        ├── {subject_id}_dpabi_smooth.nii      # 平滑后输出
        └── dpabi_subject_wrapper_result.json   # Subject 结果

reports/dpabi/
├── dpabi_subject_wrapper_summary.json          # 数据集级汇总
└── dpabi_subject_wrapper_report.md             # 可读报告
```
### 与 Step 22 的核心区别
Step 22 Step 23 数据 16×16×16 合成 NIfTI 范围 Sandbox 测试 输出位置 work/dpabi/single_function_sandbox/ 对比 无 报告 单函数审计

简单来说：Step 22 是"在沙箱里试"，Step 23 是"在真实 subject 上跑" —— 将验证过的 DPABI 单函数 wrapper 应用到实际 subject 数据，并生成可对比的工程报告。
'''